# 项目约定 · AI 助手必读（唯一权威）

> **任何改动前先读本文件。** README 只讲"怎么装 / 怎么用"，**取数规则、业务口径、工程纪律一律以本文件为准**。
> 本文件随仓库同步到 GitHub，**换电脑 / 换会话后依然有效**（本地 `.workbuddy/memory/` 不入库，只有这里是持久的）。
> 最后更新：**2026-09-22**（本节时间戳会变，判断时效以「§0.1 与代码的实际一致性」为准）

---

## 0.1 · ⚠️ 唯一事实来源（Source of Truth）

**本文件是唯一权威。** 历史上「同一个错误反复犯」全部源于**多处记忆互相矛盾**——请按下面处理：

| 信息源 | 位置 | 状态 |
|---|---|---|
| ✅ **本文件** | `stock_dashboard/项目约定.md`（已入 git） | **唯一权威，冲突时以它为准** |
| ⚠️ 本地 memory | `神秘力量/.workbuddy/memory/*.md` | **只是副本，不入库，会过期**。冲突时以本文件为准，并把本文件内容抄回去 |
| ⚠️ 自动化提示词 | WorkBuddy 系统内 | 各任务自带一份操作说明，**可能与本文件不一致**——发现不一致要改提示词（不能用文件操作改） |

**开工三步（每次新会话必做）**：
1. 读本文件（至少 §0.1 / §2 / §3.9b（日K线涨停标黄）/ §3.14 / §5 / §9）。
2. `git log --oneline -10` 看最近改了什么。
3. 动手前**先核对代码现状**（`grep` 一下函数/字段是否还在），**不要凭记忆改**——记忆里说的东西可能已被删除。

> 🔴 **最常见的重犯错误**：照着本文档的旧描述「把已删除的东西加回来」。
> 下面每一处**刻意删除**的都会标注「⛔ 已删·别再往回加」。见到这个标记 = 不要恢复。

---

---

## 0.2 · 项目概况

- 形态：**单页 HTML 看板 + Python 数据管线**，无后端服务。四个模块：每日必看 / 梯队复盘 / 重点板块 / 均线共振池。
- 目录：`D:\软件\WorkBuddytwo\神秘力量\stock_dashboard`（= git 仓库根）
- 线上（GitHub Pages，永久链接、无休眠）：https://JIANWANGSAN.github.io/stock-dashboard/
- 仓库：https://github.com/JIANWANGSAN/stock-dashboard
- 本地跑：`setup.bat`（首次）→ `refresh.bat`（刷新数据）

---

## 1 · 浏览器取数：跨域规则（最易踩坑，务必遵守）

| 数据 | 源 | 浏览器取法 |
|---|---|---|
| 个股分时 / 日K | 腾讯 `web.ifzq.gtimg.cn` | **直接 `fetch`**（返回 `Access-Control-Allow-Origin: *`）|
| 个股分时 / 日K（兜底）| 同花顺 `d.10jqka.com.cn`（`hs_<码>`）| **script 注入 + 固定回调名** |
| 板块指数 日K / 周K | 同花顺 `d.10jqka.com.cn`（`bk_<码>`）| **script 注入 + 固定回调名** |
| 板块榜 / 涨停池 / 新闻 / 量能 | 同花顺 | 后端 Python 抓，不受跨域限制 |

**核心结论：同花顺 `d.10jqka.com.cn` 返回的是 `quotebridge_v6_*(...)` 形式的 JS 回调，不是 JSON。**

- 正确姿势：**先挂同名全局函数**，再注入 `<script>` 取数 —— **不受同源策略限制**，且 HTTPS 页面不会触发混合内容拦截。
- 回调名是**固定**的，由前缀/代码/周期拼出：
  - `quotebridge_v6_line_<bk|hs|zs>_<码>_<01日|11周|12周>_last`（K线）
  - `quotebridge_v6_time_<hs_码>_last`（分时）
- `index.html` 已有封装 **`thsCall(url, cbName, timeoutMs)`** 与 **`thsBoardGet(thsCode, period)`**，新需求直接复用。
- ⚠️ 周K 有 **`11` / `12` 两个分片**，部分板块只认其一 → `thsBoardGet` 内部依次尝试。
- 这正是历史上两次"点开空白 / 加载失败"的同一病根（个股弹层、板块K线）。
- **东财路径已整体删除**（`jsonpGet` / `parseEm` 均不再使用）。
- 站点**无 CSP**；`sw.js` 对跨域请求一律放行（`url.origin !== self.location.origin → return`）。
- ⚠️ **东财 `push2his` 教训（归档）**：不返回 `Access-Control-Allow-Origin` → 浏览器 `fetch` 必被拦，且**按 IP 封禁 K线路径**（同主机其它接口正常）、**抽风式偶发放行**（会导致整批数据被坏值覆盖）。故源站已弃用。

---

## 2 · 盘前 / 盘后分工（自动化时间表）

> **当前活跃的股票类自动化只有 3 条**（2026-09-20 实测 WorkBuddy 后台核对）。§6 是重建清单。

| 时间 | 任务 | 脚本 / 动作 | 职责 |
|---|---|---|---|
| **9:26** | 盘前刷新（连板候选池 + 竞价判定）| `python premarket.py` | 重算 `candidates` 候选池 + `recommend` 次日首选 → 抓竞价 → 写回 `candidates[].auction_*` |
| **15:05** | 复盘 + 仪表盘刷新 | `fetch_data.py` → `enrich_tags.py` → `module4.py` → 发布 → 复盘文本归档 | 指数/量能/新闻/板块榜/连板梯队/三类节点/模块4/**必看页 macro（内置第 [7/8] 步）**。**不得重算候选池与推荐**，只沿用盘前那版 |
| **15:35** | GitHub 每日备份同步 | `git add -A && commit && push` | 纯备份，**不跑任何抓取脚本、不改代码** |

- ⛔ **「9:10 盘前新闻刷新」（`refresh_news.py --days 3`）已于 2026-09-18 删除**，**不要再重建**。
  原因：必看页「消息速览」卡片已从界面移除，该产出不再展示；`news` 四分类数据由 15:05 的 `fetch_data.py` 顺带产出，供复盘文本引用即可。
  `refresh_news.py` 文件仍在仓库（未删），但**没有任务调用它** —— 将来若确实要单独刷新闻再说，**现在不要加回时间表**。
- `fetch_data.py` 第 **[8/8]** 步已改为**沿用上一版** candidates/recommend（由 9:26 盘前任务负责）。
- `enrich_tags.py` **不再重算推荐卡**（否则会覆盖盘前的实战口径）。
- **盘中（9:30–15:00）绝不要跑全量 `fetch_data.py`**：会把不完整的盘中数据写进 `board_history.json` 与 `nodes.json`，**污染板块累计榜与节点池**。

---

## 3 · 核心口径（改代码前必读）

### 3.1 股票池过滤
一律剔除 **北交所（4/8/92 开头）、科创板（688/689）、ST / 退市**。统一用 `fetch_data.is_excluded(code, name)`，**任何新增股票来源都要过这一层**（模块4 曾漏掉，混入沃尔德/奥浦迈）。

🔴 **例外：「连板高度梯队」还要额外排除创业板**（2026-09-23 用户要求「排除掉创业板的部分」）。
- 创业板（300/301）在全站口径里是**保留的**（`ths_source` 的涨停判定要区分 20% 限幅），**别改 `is_excluded()`**。
- 单独用 **`fetch_data.is_cyb_bk(code)`**（创业板/科创板/北交所 → True），**只用在 `build_ladder()`**。
- 口诀：**全站剔北交所+科创板+ST；连板梯队再多剔一个创业板**（= 只留主板）。

### 3.1b 连板高度梯队 / 跌停板梯队（模块2 可视化）
- **连板高度梯队**（图）：近 7 个交易日，**只画「当日最高板」这一条线**，
  用来**看最高板高度在一个时间段内怎么变化**（升 = 情绪加速，降 = 退潮断板）。
  - 🔴🔴 **2026-09-23 用户最终要求（以此为准）**：
    > 「**连板高度这个是改成只跟踪最高板的一条线**」
    - 图上 = **只有一条线**：`Y = 当日最高板的板数`（每天一个点，`smooth:true` 平滑曲线连接）；
    - 点上数字 = **当日最高板的家数**（= `top_n`）；
    - tooltip（`trigger:'axis'`，鼠标移到任意一天）→ 列当天最高板的**全部股票名 + 所属概念**；
    - **只统计主板**（创业板 300/301、科创板 688/689、北交所 4/8/92 全部剔除）；
    - 纵轴 = 板数轴，`min:0`、整数刻度，`axisLabel.formatter:v=>v>0?String(v):''`（不显示 0 的文案）。
  - 🔴 **历史沿革（同一需求被反复改坏 9 版，**别再往回走**）**：
    | 版本 | 做法 | 结局 |
    |---|---|---|
    | v1 | 只画 `top/second/cyb` 三条线 | ❌ 用户：「9-22 的 3板 和 4板 呢？」（三条线装不下 5 个层级）|
    | v2 | 每层一条**平行线**，`connectNulls:false` | ❌ 用户：「不能改为用折线图连接吗？」|
    | v3 | 仍是平行线，只改 `connectNulls:true` | ❌ 用户：「我的意思是你做成**这样的折线连接**」（发参考图）|
    | v4 | **单条折线** + 同日**横向错开**（数值轴） | ❌ 用户：「同一天的纵轴不应该是垂直的吗？你这**全是斜的**」|
    | v5 | **单条折线** + 同日**共享同一 x**（类目轴、竖直） | ❌ 用户：「你还是没懂……**不是让你重新做**」|
    | v6 | **每层一条独立曲线** + 缺层落 0 轴 + 只主板 + 只 ≥4 板 | 用了一段时间 |
    | v7 | v6 + Y 轴刻度收成 `0/4/5/6/7/8/9` + 配色「最高板优先 紫红黄深蓝绿青灰」 | 用了一段时间 |
    | ~~v8~~ ❌ | **改成单条「最高板主线」**（每天只取当日最高板） | ❌ 用户：「你把每层一条线删了干嘛？」→ **已回滚**（当时理解错了）|
    | v9 | v7 形态 + 删掉 0~3 的横线与刻度（空轴不保留） | 用了一段时间 |
    | **v10 ✅** | 🔴 **只跟踪最高板一条线**：`Y=top`、点数字=`top_n` | **当前口径**（用户 2026-09-23 明确要求）|
    ⚠️ **v8 与 v10 都是「单条线」，但来历完全不同，不要混为一谈**：
      · v8 = 我**自作主张**把「每层一条曲线」改成单条主线 → **被否**；
      · v10 = 用户**明确要求**「改成只跟踪最高板的一条线」→ **当前口径**。
    ⛔ **不要再恢复「每层一条独立曲线」**（v6/v7/v9 形态）—— 用户已明确弃用。
    ⛔ **也不要做「同日横向错开 / 同日共享 x 的粗折线」**（v4/v5，被否）。
  - 数据：**只用 `ladder[*].top`（板数）/ `top_n`（家数）/ `top_list`（名单+industry）**。
    ⚠️ `second` / `cyb` / `levels` **仍在 `data.json` 里**（别删，其它模块还在读），但**连板图不再用**。
    ⚠️ 后端 `build_ladder()` 的 `levels` 门槛 **`b >= 4`** 仍保留（`check_ladder.py` 与别处读取）；
      **前端图表不再消费 `levels`**。用户曾报「09-22 新华文轩 3 连板没抓到」——
      **不是抓取 bug**：同花顺连板梯队接口 09-22 明确返回 `height=3` 含新华文轩，
      只是当时 `levels` 门槛 `b>=4` 把它过滤在了图表外；改为单条 `top` 线后此问题不复存在。
  - 渲染：`drawLadderChart(el, prevChart, L, cfg)` 的 **`levelMode:true`** 分支 —— 只建 **1 条** series，
    `name: cfg.labels[0]`（=「最高板」）、`data: L.map(s=>({value:s.top||0, cnt:s.top_n||0, list:s.top_list||[]}))`、
    `smooth:true`、`lineStyle/itemStyle.color = cfg.colors[0]`（`#2563eb`）、`grid.right = 34`。
    🔴 **点数字用「逐点 label」**（`d.label={show:true, formatter:String(d.cnt)}`），
      ⛔ **不能只写 series 级 `label.formatter`** —— 本机 ECharts 5.4.3 **不渲染**（实测）。
    ⛔ 不用 `visualMap`；`setOption(opt, {notMerge:true})`（默认合并会残留旧配置）。
  - **回归断言（`_shot.js`，双视口）**：`series 数 == 1`、`name == '最高板'`、
    `Y 值逐点 == 各日 top`、**逐点 label == 各日 top_n**（ZRender `tspan` 按 `style.fill == #374151` 取）、
    `smooth == true`、`lineStyle.color == '#2563eb'`、`top_list` 无 300/301/688/689/4/8/92 开头、
    无 `visualMap`、tooltip `trigger=='axis'` 且含日期 + 当天全部股票名、note 无 `**`、
    卡片 tag 含「只跟踪最高板一条线」。
  ⚠️ 测 ECharts **必须读 option / ZRender 图元**（headless Edge 拍 canvas 会全白 → 假失败，见 §9.3.1）。
  ⚠️ **ZRender 里文字图元 `type === 'tspan'`**（不是 `'text'`）—— 遍历时两者都要收，否则收不到任何文字。
  ⚠️ **区分「y 轴刻度文字」与「点上家数标签」按 `style.fill` 色**：刻度 `#6E7079`、家数标签 `#374151`。
    不能按坐标区分（`getBoundingRect()` 都是**局部坐标**，x 都是 -6 / -3）。
  ⚠️ `getOption()` **不回读函数**（`formatter` 读回 undefined）→ 凡是要验函数行为的，都得实渲染。
- **跌停板梯队**（图，紧跟连板图之后，**与连板图不再同构**）：
  - 仍走 `drawLadderChart()` 的 **`levelMode:false`** 分支：三条线 = 最高连跌 / 次高连跌 / 创业板；
  - 数据源 `dt_ladder`（`build_dt_ladder()`），**无 `levels`** → 天然不触发 `levelMode`。
  - 点数字 = 当日该梯队**连续跌停天数**；tooltip 附注「当日跌停 N 家」。

### 3.1c ⛔ 「连板候选池」卡片 —— **前端已删（2026-09-23 用户要求）**
- 用户指着该表说「这个功能删掉吧，不要了」→ 已删：HTML 卡片 + CSS（`#cand-box` 列宽/手机端）
  + `renderCandidates()` + `init()` 里的调用 + `.auc-ok`/`.auc-no` 样式。
- ⛔⛔ **只删前端展示！后端 `data.candidates` 必须照旧全量产出。** 它还有 4 个后台消费方：
  | 消费方 | 用途 |
  |---|---|
  | `auction_check.py`（9:26 竞价核对） | 逐只读 `candidates` → 写回 `auction_*` 字段 |
  | `build_reco()` | 用候选池**第 1 名**生成「次日连板推荐」卡（`reco-box` 仍在页面） |
  | `module4.py::get_fallback_members()` | 拿它当题材兜底池 |
  | `check_ladder.py` 第三道 `check_candidates()` | 断言入池门槛 `boards>=2` |
- **绝不可**顺手删 `build_candidates()` / `data['candidates']` / `premarket.py` 里的 `cands` 写回。
- 🧪 断言（`_shot.js`）：`#cand-box` 不存在、正文无「连板候选池」标题、`renderCandidates` 为 `undefined`、
  **但 `data.candidates` 仍在且 >0**、推荐卡仍渲染。


### 3.2 节点票池（三类节点，只看最高标）
- **节点口径**：① `最高标断板`（断板日量 vs 涨停日量：缩量/暴量出货均落首板）② `突破`（连板高度创阶段新高当日）③ `穿越`（最高标**跨过昨日由别的票创下的天花板**）——均取**当日首板**为节点票。
- 🔴🔴 **穿越节点的补充规则（2026-09-23 用户明确，最容易搞反）**：
  > 「**穿越自己不产生新节点**，比如华瓷 5 板是穿越了澳弘电子，6 板是华瓷自己，
  >  那么就算往上打高度，**不产生穿越节点**」
  - **判据 = 昨日最高板（`max_prev`）的持有者只有今日最高标自己一只** → `self_only=True` → **跳过，不出节点**。
    （即「天花板是它自己昨天的高度」 = 纯自续板，不算跨越了别人的坎。）
  - ✅ 案例（实测）：**09-21 华瓷 4板→5板**：昨日(09-18) 4板是「**锡华科技 + 华瓷股份**」**并列**
    → `self_only=False` → **算穿越**（它跨过了同梯队另一只票的肩）；
    **09-22 华瓷 5板→6板**：昨日(09-21) 5板**只有华瓷自己** → `self_only=True` → **不产穿越节点** ✅
  - ⚠️ 等价的自续板（如 09-16 闽东电力 5→6、09-07 龙版传媒 5→6）同样**不再产节点** —— 规则统一应用，不是特例。
  - 代码位置：`fetch_data.py` 的 `detect_nodes()` 里
    `prev_top_codes = {s['code'] for s in prev_pool if s['lbc'] == max_prev}` +
    `self_only = bool(prev_top_codes & top_codes) and len(prev_top_codes) == 1`，
    条件写 `if crossed and first_boards and not self_only:`。
  - ⛔ **不要退回「今日最高板 > 昨日最高板」的老判据** —— 那会把「自己往上打」也记成穿越（就是这次的 bug）。
  - ⛔ **也不要用「20 日回看窗口里不含本票的最高板」当天花板**（试过，过度收紧）：
    窗口里有历史 7 板（深中华A）时，后续所有票的 `other_max` 恒为 7 → **所有穿越节点全灭**（实测）。
- 只保留**总市值 ≤ 200 亿**；只保留**最近 10 个交易日**（约两周）。
- 节点内**已无任何连板票**（连板中 / 断板反包）即自动删除；当日新节点豁免。

### 3.3 断板判定
一路未断板 → `连板中`(≥2板) / `首板`；断板后 **≤3 个交易日**重新涨停 → `断板反包`；今天不在涨停池 → `已断板`（UI 折叠）。

### 3.4 概念抓取
同花顺 **F10**（`basic.10jqka.com.cn/<码>/concept.html`，GBK）→ 概念名 + 概念ID + 地域 + 申万行业 → 别名归一（如 `CONCEPT_FIX` 里按代码锁定）→ 由榜单热度排出「当下最热概念」；再叠加**同花顺涨停原因**（`block_top.reason_type` / `reason_info`，见 §3.13b）。个股概念不准时，在 `fetch_data.py` 的 `CONCEPT_FIX` 里按代码锁定。
> 已不再使用东财 `push2/slist` 与 `datacenter F10`。

### 3.5 上板分时量（⚠️ 曾出重大 bug，务必按此口径）
**= 封板那一刻所在「那一根分时线」的成交额（该分钟增量，不是累计）。**

- 分钟线标签 = 该分钟区间的**结束**时刻（首根 09:30 = 集合竞价）。
- 源站 `fbt` 的分秒**常被截断**，故用 **`seal_bar_label(fbt)` 一律进位到下一分钟**再取那根。
- 正例：闽东电力 `fbt=93024`（09:30:24）→ 取 **09:31** 那根 = **2.35 亿**（硬线 1.17 亿）。
- 反例（旧 bug）：把 `93024` 截成 `09:30` → 命中**集合竞价那根**（开盘价 14.51）→ 只算出 0.82 亿，**量能严重低估**。已于 2026-09-14 修正。
- **数据源＝同花顺分时** `d.10jqka.com.cn/v6/time/hs_<码>/last.js`，字段序 `时间,现价,该分钟成交额(元),均价,该分钟量`（第 3 列 = 增量成交额，与东财 f57 语义一致，已抽样验证）。
- ⚠️ **盘后 15:05–15:30 该文件行尾列可能为空** → 必须逐行 `try` 容错，单行失败跳过。
- ⚠️ 若 `fbt` 缺失（同花顺涨停原因覆盖不到的票），**从分时反推封板时刻**：取「现价 == 当日分时最高价」的那根。
- 炸板票统一取**首次封板**那一根；炸板回封不重复计入。

### 3.6 竞价硬线与合格判定（并入候选池）
- **硬线 `bid_required_yi` = 上板分时量 × 50%**；**全量候选都算**（6 线程并发 + 当日缓存）；只有分时取数失败才回退 `全天成交额 × 50%`，回退时该格悬浮提示会标出 `bid_basis`。
- **合格判定**：`竞价额 ≥ 硬线` → 合格。结果写回每条候选 `auction_amt_yi` / `auction_ok` / `auction_open` / `auction_pct`。
- ⚠️ **只有 9:15–9:30 抓到的量额才是真竞价额**，其他时段拿到的是**全日成交额、不可比**；`auction_is_live` 标记是否真竞价口径，卡片会如实标注。
- 数据源（2026-09-20 起，**东财已从本链路彻底移除**）：
  **腾讯 `qt.gtimg.cn` 快照为主（今开 / 量 / 额 / 昨收）→ 同花顺 `realhead` 兜底**（`auction_check.ths_quote()`）。
  · 错过 9:15–9:30 时，用**同花顺当日分时首根（09:30 = 集合竞价）**还原真竞价额（`ths_source.fetch_minute_auction()`）。
  · ⚠️ **分时首根必须校验日期**：同花顺盘前 / 非交易日返回的是**上一交易日**的分时，
    不校验就会把昨天的集合竞价当成今天的（`auction_check.ths_first_bar()` 已做，`data.date` 对不上直接丢弃）。
  · ⚠️ 本链路原先走「东财 push2 快照 + **push2his** trends2 分时」—— push2his 正是 §1 标注「按 IP 封禁」的路径，
    一旦被拦会**静默降级**到腾讯累计口径、`auction_is_live` 失真。已于 2026-09-20 移除。
- ⚠️ `auction_is_live` / `auction_source` / `auction_updated` / `premarket_updated` 是**顶层键**，
  由 9:26 的 `premarket.py` / `auction_check.py` 写入；15:05 的 `fetch_data.py` 是**整字典重建**（`data = {...}`），
  必须显式搬运 —— 2026-09-20 修：此前这 4 个键会被 15:05 **整批抹掉**，前端「竞价口径」标注永远拿不到值。

### 3.6b 🔴 连板候选池的**入池先决条件**（2026-09-22 用户明确，最容易搞错）

**规则：候选池是「连板」候选池 → 入池票必须「今天在涨停池、且连板数 ≥ 2」。**

- **理由（用户原话）**：「连板候选池起码先决条件是这个池里票**前一天要是涨停**才能是连板」。
  「今天涨停 且 连板数 ≥ 2」**必然蕴含「前一交易日也涨停」**（因为连板 = 连续不中断）。
- **等价判据**：`cur['lbc'] >= 2`（`cur` = **当日涨停池**里的该票）。
  - ⚠️ 用**当日涨停池的实时 `lbc`**，**不要信节点里存的 `boards`**（那是节点日的快照，会滞后）。
- **必须剔除的两类**：
  - **首板（`lbc == 1`）**：前一交易日没涨停 → **不是连板**；
  - **断板反包且 `lbc == 1`**：连板段中途断过、今天重新涨停 → 连板数**从 1 重新起算** → **也不是连板**。
    （若它已续成 ≥2 板，它会以 `status='连板中'` 出现在**它自己的节点**里，自然会被收到。）
- ⛔ **曾经的 bug（2026-09-22 用户第二次报，我只允许自己犯一次）**：
  `build_candidates` 第二分支只写 `if s['status'] not in ('连板中','断板反包'): continue`，
  **完全没校验 `boards`** → 一批「节点当日首板」通过它们在**旧节点**里的 `断板反包` 记录混进池子
  （同一票在多个节点出现，`dedup_candidates` 保留优先级最高的那条 = 旧节点的 `断板反包`）→
  表现 = **候选池里一堆 1 板 / 断板票**。实测 09-21 的池里混进 **6 只**（美盈森 / 福莱蒽特 / 华脉科技 /
  新亚制程 / 海洋王 / 豪尔赛，全部 `lbc=1`），把真正的连板票（龙头股份 / 华软科技 / 南华生物 /
  瑞康医药 / 新华文轩 等 3 板、2 板）挤出了 12 个名额。
- **两道保险（改这里必须都过）**：
  1. **两个分支都加 `lbc >= 2` 硬门槛**（trigger 分支 + stocks 分支），判据函数 `_ok_boards()`；
  2. **出池前收尾自检**：`dedup_candidates()` 之后再过一遍 `boards >= 2`，不满足的**打印 warn 并剔除**
     —— 保证「无论哪个分支 / 将来谁改坏，都不可能有 1 板票漏网」。
- **回归检查**：`check_ladder.py` **第三道检查** `check_candidates()` 专门盯这条
  —— 读 `data.json` 的 `candidates`，逐只断言 `boards >= 2`、在涨停池、池内 `lbc >= 2`。
  **改候选池 / 连板相关代码后必跑 `python check_ladder.py`。**
  - ⚠️ **比对用的必须是「基准日」的涨停池，不是 `data.date` 当日的池**：
    候选池由**盘前**任务生成，基准 = **生成日的前一交易日**（T 日 09:26 读的还是 T-1 收盘版）。
    15:05 盘后重建后 `data.date` 前进到 T，此时拿 T 的池去比对会**假报 8/12 违规**
    （2026-09-22 实测踩到）。`_candidate_base_date()` 已按「`premarket_updated` 日期的前一交易日」推算基准日；
    池取不到（网络）时降级为只断言 `boards >= 2`（会打印提示，不静默）。

### 3.7 炸板次数
候选池「炸板」列，取自**同花顺涨停池** `open_num`（炸板总家数）与 `block_top.stock_list` 的个股开板次数，**非 0 即标红** → 风险标记。

### 3.8 板块榜（**数据源＝同花顺**，2026-09-17 起东财已全站停用）
> THS 榜页本身**不带 3日/5日/涨停家数/领涨股**，全部由脚本补算 —— 见下。

- **行业榜**：`https://data.10jqka.com.cn/funds/hyzjl/`（第 1 页 50 行）+ `.../hyzjl/page/2/`（第 2 页 40 行）= 90 个。
  ⚠️ **旧的 ajax 分页路径 `.../page/N/ajax/1/` 已于 2026-09-18 改为 401 Unauthorized**（需登录 cookie）→ 改用上面两条**列表页**，且**必须 HTTPS**（HTTP 版会被跳转/401）。
  单行给全：`序号|名称|指数|涨跌幅%|流入亿|流出亿|净额亿|公司家数|领涨股|领涨股涨跌幅%|当前价`。
  ⚠️ 解析要用「首列是数字」过滤（表头是 `<th>`、数据行是 `<td>`）。
  ⚠️ 同花顺的「行业」= 他自己的行业分类（不是申万二级），**命名体系与东财不同**，不要再套 `is_l2_industry` 的过滤逻辑（已废弃）。
- **概念榜**：同域 `https://data.10jqka.com.cn/funds/gnzjl/page/N/`（实测 **386~387 个 · 8 页**），**只在数据层**（`board_concept` + `macro.concept_in`），供盘前「题材共振」与「资金主线」匹配用。
  ⚠️ 条数会**小幅波动**（386 ↔ 387），并非脚本 bug：列表页按**涨跌幅降序**分页，源站个股/板块集合本身在变；且并发翻页偶发空响应会让某页整页丢失
  （曾出现概念榜 386 条但缺「创新药」→ heat 匹配 26/27）。`fetch_fund_rank` 对**第 2 页起做了 3 次重试**，若仍见到整块缺失先怀疑这条。
- **3日/5日涨幅**：榜页**没有**这两列 → 由 `THS.pct_over(ths_code, N)` 按**板块指数日K自算**（近 N 根收盘累计涨幅）。
  榜单 TOP30 与产业清单并发（10 线程）补算，单次约 3~5s。字段语义已从「东财 f127/f109 官方值」变为**自算值**。
- **「涨停/全部」列**（2026-09-20 换源）：走 `fetch_data.em_board_zt_count(em_code)` —— **东财 push2 板块成分股**
  （返回自带 `f3` 涨跌幅 → 配 `_is_limit_up()` 判涨停）。因为**同花顺成分股页已被反爬拦死**（见 §8.1）。
  - `涨停` = 成分股中判为涨停的家数；`全部` = 成分股条数。
  - ⚠️ **本列实际只对「产业板块」（`sector_daily` / `sector_3d`）有效**：只有它们带 `em_code`
    （`_norm()` 给 `board_daily` / `board_concept` 的 `em_code` 恒为空串）；前端 `zt_count` 也**只在产业板块**渲染
    —— 2026-09-20 起只剩 **「产业板块 · 3日 TOP10」** 一张表会显示它（当日表已删，见 §9.1 第 16 条）。
  - `stock_total` **仍沿用同花顺行业榜的「公司家数」**（如半导体 188），仅当该字段缺失时才用成分股条数补。
  - **退路**：`THS.count_board_zt(ths_code)`（同花顺成分股页）**保留不动** —— 当前 401 恒 None，接口哪天恢复即自动生效。
  - ⚠️ **东财 `pz` 实测封顶 100**：传 500 也只回 100，按「返回条数 < pz 即尾页」判停会**只取到第 1 页**（`stock_total` 少一半）。
    涨停家数不受影响（涨停股涨幅最大、恒排第 1 页）。
  - 历史口径（已失效，勿照抄）：原走同花顺成分股页，概念页每页 10 行 / 行业页每页 20 行（曾按 20 统一乘 → 概念总家数虚高一倍）；
    桥接不到概念ID 时按指数码仍可取（早期在此 `return None` 造成 6 个板块假缺失）；并发下同花顺偶发 403 → 第 1 页需重试 2 次。
- **字段**：`name / code / ths_code(881xxx|885xxx|886xxx|概念ID) / em_code(东财BK，仅用于前端 bklink 与本地K线键) / pct / d3 / d5 / up / down / leader / leader_pct / zljlr_wan / stock_total / zt_count / amount_yi`。
- **板块名 = 板块K线代码 = 成分股 = 个股题材标签** 同源（同花顺），这是切源的主要收益。
- **两套并行的同花顺板块编码（极易混用）**：
  - 板块**指数码** `881xxx`(行业) / `885xxx|886xxx`(概念) → 用于 `d.10jqka.com.cn` 的 K线(`line/bk_*`)与快照(`realhead/bk_*`)
  - 概念**板块ID** 6 位（如 `309049`=CPO、`308014`=创新药、`308642`=数据中心）→ 用于 `q.10jqka.com.cn/gn/detail/.../code/<ID>/` 取**成分股**
  - 两套**名称完全一致** → 用「名称桥接」互相解析（`THS.board_ref()` / `concept_id_by_name()`，数据在 `_ths_boards.json`）
  - 已知无概念页：`885927 CRO概念`（成分股仍可按指数码取，只是没有 gn/detail 页）；`AIGC概念` 无同名 → 别名桥到 `人工智能`(`CONCEPT_BOARD_ALIAS`)。

### 3.8b 「产业板块」清单（模块3 实际展示的就是这 28 个）
- **用户明确要求**：模块3 不要东财全部二级行业，而要**他行情软件里的「产业板块」那 28 个**。
- 实现：`fetch_data.SECTOR_WATCH` = `[(展示名, 东财BK代码), ...]`，共 28 个；
  `fetch_board_rank` 里这些代码**无条件放行**（不受"剔一级/三级/风格"过滤影响，否则会漏掉 机器人/锂电池/银行/稀土/石油 这 5 个）。
  ⚠️ **必看页热力网格（`macro.fetch_heat()`）复用同一份清单，但实际渲染 27 格** ——
  差的 1 个是 `新消费`（东财 BK1652，同花顺无对应板块）→ 拿不到快照自然跳过。
  **所以 「`SECTOR_WATCH` 28 条」与「`macro.heat` 27 条」都对，不是缺数据**（校验时别把 27 当 bug）。
- 字段 `data.sector_daily`（按当日涨幅降序）/ `data.sector_3d`（按 3 日涨幅降序）；展示名用清单里的短名（如「存储」而非东财「存储芯片」）。
- 前端：模块3 产业板块部分**只展示「产业板块 · 3日 TOP10」一张表**（2026-09-20 起；「当日 TOP10」整表已按用户要求从前端删除，
  见 §9.1 第 16 条）。表内含 **★ 重合标记**（与当日榜重合 → 持续性强）、**「涨停/全部」列**、板块名可点看日K/周K；
  下方「小结 · 主线 vs 脉冲」给"★重合 / 单日脉冲"。
  - ⚠️ **★ 与小结仍依赖 `data.sector_daily`**（当日榜只删了**展示**，数据照旧产出）——**别顺手把数据也删了**。
  - ⚠️ **3日表列宽必须合计 100%**（现为 8/34/20/16/22）。旧值 9/50/26/15/22 = 122% 溢出，
    会把最后一列「涨停/全部」挤成 1 个字宽、`0/188` 竖着折行（2026-09-20 修）。列宽**只在 `nth-child` 一处定**，class 别再重复定宽。
  - ⚠️ **改这份清单的过滤/形态时：TOP10 与 ★ 的机制必须保留**（曾误删 TOP10/★，被用户纠正）。
- ⚠️ **改这份清单时要照用户截图逐一核对代码**（如 无人驾驶→东财「智能驾驶」BK0802、黄金→「黄金概念」BK0547 而非「黄金」BK1617、电网→「电网设备」BK0457）。
- 改清单后**必须把新板块加入** `_stat`（涨停统计）与 `_kl`（K线预生成），否则「涨停/全部」和 K线会缺。

### 3.9 板块K线（板块名可点击）
模块3 的板块名可点，弹 **日K / 周K + MA5/MA10/MA20**。
- **数据来源＝盘后预生成**（不依赖浏览器直连）：`fetch_board_klines()` 盘后把 28 个产业板块的**日K120根 + 周K60根**
  抓好写进 `data.js → board_kline`，前端点击**直接读本地**（秒开、可离线）。本地没有再走网络兜底。
- ⚠️ **数据源已统一为同花顺**（2026-09-17 用户明确要求停用东财）：开关在 `board_kline_src.SOURCE_PREF = 'ths'`。
- 预生成失败/残缺时**沿用上一版** `board_kline`，绝不清空（`merge_board_kline`）；手动补齐用 **`python fill_board_kline.py`**（增量、可反复跑）。
- 历史教训：东财 `push2his` **无 CORS 头**，浏览器 `fetch` 必被拦、JSONP 也时好时坏 → 所以必须盘后预生成。
- 历史教训：**取东财K线必须加时间戳 `&_=<毫秒>`**（不加会被限流返回 None）。

#### 3.9.1 数据源＝同花顺（东财已停用）
- **取数实现集中在 `board_kline_src.py`**（`fetch_data.py` 与 `fill_board_kline.py` 共用，别在两处各写一套）：
  · `SOURCE_PREF = 'ths'`：**只用同花顺**。设 `'em'` 可切回东财，`'auto'` 才会先探测东财。
  · `THS_MISSING_FALLBACK_EM = True`：同花顺**没有对应板块**时（目前只有 `BK1652 新消费`）允许单独用东财补这一条；设 False 则留空。
  · `merge_board_kline()`：新数据残缺/空返回的板块**沿用老数据** —— 防「数据源抽风」把整批好数据覆盖成个位数。
- **同花顺接口**（HTTPS 可用，脚本与前端都能取）：
  · 板块：`https://d.10jqka.com.cn/v6/line/bk_<码>/<01=日|11=周>/last.js`
  · 指数：`https://d.10jqka.com.cn/v6/line/zs_<码>/01/last.js`（`zs_1A0001` 上证 / `zs_399106` 深证综指）
  · 个股：`https://d.10jqka.com.cn/v6/line/hs_<6位码>/01/last.js`；分时 `.../v6/time/hs_<码>/last.js`
- **映射关系**在 `ths_board_map.py`（东财BK → 同花顺码，27 条），由 **`python build_ths_map.py`** 重建：
  同花顺没有公开板块代码表，但 **K线接口回带有 `name` 字段**，故扫码段 `881100-881301 / 885300-886400`
  建「名称→代码」全表，再按人工确认的别名表对齐。
- ⚠️ **字段序不同（极易错）**：同花顺是 `日期,开,高,低,收,量,额`，东财是 `日期,开,收,高,低,量`。
  统一换成 **`日期,开,收,高,低`**（前端 `renderBoardK` 按此解析）。
- ⚠️ **周K 有 11/12 两个分片**：同一份周K数据，部分板块只有其一可访问（`11` 报 HTTPError 时 `12` 往往可用）→ 依次尝试。
- ⚠️ **量能也已改同花顺**：`fetch_market_volume()` 用 `zs_1A0001` + `zs_399106` 的日成交额（第 7 列，单位元）。
  同花顺日期是 `YYYYMMDD`，**必须格式化成 `YYYY-MM-DD`** —— 否则与东财口径混用会打乱排序、且「剔除未收盘当日」失效。
- ⚠️ **前端兜底顺序**：①本地 `board_kline` → ②同花顺（`THS_BK_MAP` + `thsCall`：script 注入 + 固定回调名
  `quotebridge_v6_line_bk_<码>_<周期>_last`）→ ③「暂无K线」。个股同理：腾讯优先 → 同花顺兜底（`parseThsK/parseThsMin`）。
  **东财路径（`jsonpGet`/`parseEm`）已整体删除**。
- ⚠️ **同花顺日K盘中不含当日**（当日行要么没有、要么是昨收占位）→ 15:05 盘后跑才拿得到当日；
  实测**板块指数日K当日行比个股更晚发布**（2026-09-22 16:47 仍止于上一交易日，日K 25/28 个板块末条=09-21）→
  当日缺口是**正常现象**，当晚或次日重跑才会补上。
- 🔴 **要"刷新落后一天"的板块K线，必须用 `--force`**：
  `python fill_board_kline.py --force`（只处理页面展示的 28 个产业板块，整批重取日K+周K，会覆盖写回）。
  ⛔ **默认的 `python fill_board_kline.py` 补不了这个**：它只补「**空**」的
  （`need_d = not v.get('day')`，见 `fill_board_kline.py` 第 49-51 行，注释写的是"非空即视为已拿到，不再反复重试"）
  → 对「末条落后」的数据会直接报 `待补 0 个：无 / ✅ 已全部齐备`，**给了你一个假的安心**。
  （2026-09-22 踩到：`fetch_data.py` 的 warn 提示"稍后重跑 fill_board_kline.py"，但那条命令**根本不会重取**。）
- **教训**：板块 K线**不要**用「多轮循环硬刷」补齐（原 `for i in 1..5` 那种）——刷不出结果还会把 IP 打进黑名单。
  正确做法是**低并发 + 单一数据源 + 失败即换源**。

#### 3.9.2 同花顺 iFinD 连接器（会话内可用，但进不了脚本）
- iFinD MCP（`get_security_indicators`，`market="指数"`）能直接给**同花顺板块指数**日频 OHLC，
  代码后缀 `.TI`（如 `886033.TI`=共封装光学CPO、`881121.TI`=半导体、`885927.TI`=CRO概念）。
- **与网页接口同源同值**（实测 `886033.TI` 20260916 收盘 6182.574 = `d.10jqka.com.cn` 同值；半导体 16623.508 亦同）
  → 可用于**会话内校验**已落的 K线、查板块指数代码、或追问某板块某日的量价。
- ⚠️ **不能替代自动化取数**：MCP 只在对话会话里可用，每日 15:05 定时任务跑的是 Python 脚本，
  **脚本只能走 HTTP 接口**（同花顺 `d.10jqka.com.cn`）。iFinD 用于「人工核查 / 找代码」，不写进流水线。
- ⚠️ `market="板块"` 返回的是**成分股聚合均价**（算术平均/加权平均收盘价），**不是板块指数**，
  别拿它当K线用；要板块指数必须用 `market="指数"`。
- 用途示例：`新消费`(东财 BK1652) 同花顺网页接口无对应，iFinD 能查到它是 `995013.SSI`，
  但该代码在 `d.10jqka.com.cn` 取不到（仅支持 `bk_`/`hs_`/`zs_` 前缀）→ 这个板块仍走东财单条补。

### 3.9b 🔴 日K线的「涨停」标记（黄色）—— 全项目统一（2026-09-22 用户指定）

> 用户原话：「在项目里所有抓取的日K线，只要当天是涨停的，就把内条K线显示为黄色……**以后都这样**。」

**规则**：任何**个股日K线**图，某日**收盘涨停** → 该根K线整体显示为**黄色**（阳线填充 `#facc15` + 边框 `#ca8a04`；**一字板也必须是黄色**，否则只剩一条线看不清）。
用途：一眼扫出该股的涨停 / 连板结构（实测闽东电力能直接看到 6 连板的黄色阶梯）。

**实现位置（只此一处，新增日K图必须复用）**
- `index.html`：`LIMIT_UP_STYLE` / `LIMIT_UP_NOISE` / `limitRatioOf(code,name)` / `isLimitUpBar(b,prevC,code,name)` —— **判据与配色都在这里**。
- `renderKline(el, res, code, name)` —— 个股日K线弹层。⚠️ `code/name` 必须传（判 ST 与板块限幅要用），调用处见 `drawStock()`。

**判据（唯一口径，改前必读；两条同时成立）**
1. **收 == 高** —— 封板到收盘。**盘中炸板（收 < 高）不算涨停**，与用户「只要当天是涨停的」一致。
2. **`|收/前收 − (1+限幅)| ≤ 0.005/前收 + 0.0004`**
   - `0.005/前收` = **涨停价四舍五入到「分」**带来的结构性偏差（自动随价位缩放：低价股偏差大、高价股偏差小）。
   - `+0.0004` = 数据源噪声预算。腾讯**前复权是 3 位小数近似**（`5.677→6.247`），实测把偏差抬高 0.0004~0.0008。

**限幅按代码前缀判定**（`limitRatioOf`）：主板 `60/00` = 10% ｜ 创业板 `30` = 20% ｜ 科创板 `688/689` = 20% ｜ 北交所 `8/4/92` = 30% ｜ 名字含 `ST`（主板）= 5%。

⛔ **禁止用「涨幅 ≥ 9.8%」这类固定阈值**（也不用 `>= 9.9%`）。原因：
- 各板块限幅不同（10 / 20 / 30 / 5%），一个阈值覆盖不了；
- 低价股涨停后涨幅**可能只有 9.66%**（涨停价向下取整，如 1.44 → 1.58），而高价股"差一分没封"就有 9.99% —— **阈值法两头都错**。

**实测标定（2026-09-22，以同花顺涨停池为真值）**
120 只票 × 近 10 个交易日 **1194 根 K 线** → **召回 174/174 = 100%，误标 1/1194 = 0.097%**。
- 唯一误标：`000002 / 2026-09-21`（+9.94%，收盘=最高）—— 属「涨停价向下取整」与「差一分没封」的**天然歧义**，数据源只有「分」精度，无法消除。
- 已知边界：**除权日**（复权因子跳变）可能漏标，已用「一字板兜底」覆盖大部分；前收 ≥ 12.5 元的票若「差一分没封且收盘=最高」会被标黄（实测 1194 根里仅 1 例）。

**适用范围**
- ✅ **个股日K线**弹层（点任意股票名 → 日K线）。
- ⛔ **板块K线不标**：`renderBoardK` 画的是**板块指数**，指数没有涨跌停概念（代码内已注释）。
- ⛔ **分时图不标**；⛔ **量柱不标**（保持红涨绿跌，只改 K 线本体）。

**回归检查**：`_shot.js` 的「日K线涨停标黄」断言组（判据 8 条 + 集成 3 条，双视口跑）——
用真实行情样本锁死：一字板命中、前复权 3 位小数命中、+9.07% 未封不命中、差一分没封不命中、一字跌停不命中、首根K线（无前收）不命中、创业板 20%/仅 +10% 区分、限幅识别；
并从 ECharts 实例读回「哪几根带黄色 itemStyle」验证真的上屏了。

### 3.10 板块发酵 & 题材共振（候选池排序依据）
- **板块发酵 = 候选题材 × 当日涨停池细分行业（同花顺 `block_top.stock_list[].reason_type` / F10 申万行业）** 模糊匹配命中的涨停家数/连板数。
  ⚠️ 同花顺行业名同样会被截断 → 匹配保持「整串互相包含 + **前 2 字词干**互相包含」，并把候选自身细分行业纳入比对，否则发酵列会长期假 0。
- **题材共振 = 候选题材 × Top30 板块涨幅榜**模糊匹配。
- **候选排序**：发酵家数 → 板数 → 主线共振。

### 3.11 ⛔ 厄尔尼诺阶段激活 —— **整块功能已删（2026-09-20）**

- 该功能（静态名单 `EL_NINO_DATA`、题材关键词 `EL_NINO_THEME_KW`、`calc_elnino_stage()`、
  `discover_elnino_leaders()` / `merge_elnino_dynamic()`、跨交易日缓存、以及前端置顶的
  `#elnino-card`「厄尔尼诺事件 · 主题轮动地图」）**已按用户要求整体删除** —— 详见 §9.1 第 15 条。
  **不要再恢复**，也不要用别的形式把「厄尔尼诺主题轮动」放回前端。
- 本节原有口径（「动态收录必须先于判定执行」、闽东电力 4 连板激活不了电力线的教训）随功能一同作废。
  **保留此标题只为让 §3.12 起的编号不变**，改动时别顺手把它删了又对不上号。

### 3.12 沪深量能
`量能 = 上证综指 + 深证综指 的日成交额`（**不含北交所**），取近 15 个交易日；柱色 红=较昨日放量 / 绿=缩量，橙色虚线=3 万亿。

**当日是否存在，取决于跑的时刻**（不是"永不纳入"）：
- **15:00 之后跑** → 列表**末条 = 当日**（腾讯快照补进来的），这是正确状态。
- **盘中（<15:00）跑** → 当日**没有**（腾讯快照字段 37 此时为空，`fetch_tx_turnover_today()` 返回 `None`），末条 = 上一交易日。

数据源＝**同花顺指数日K 出历史 + 腾讯快照补当日**（`qt.gtimg.cn` 字段 **37**＝成交额/万元）：

1. 同花顺指数日K（`zs_1A0001` 上证 / `zs_399106` 深证综指）第 **7** 列＝成交额（元）→ 出**历史** 15 日。
   ⚠️ 同花顺日期是 `YYYYMMDD`，**必须格式化成 `YYYY-MM-DD`** —— 否则与历史口径混用会打乱排序、且「剔除未收盘当日」失效。
2. **当日一律用腾讯快照补齐/覆盖**（15:00 后才可信，盘中 `fetch_tx_turnover_today()` 返回 `None`）。

> ⚠️⚠️ **同花顺指数日K 盘后不含当日**（15:00 收盘后仍停在昨日）→ 只靠日K 会永远「差一天」，表现为**「今天的量能不刷新」**。
> ⚠️ **历史踩坑（已修）**：旧版先补当日、又在末尾执行「排除未收盘的当日（`hour<15`）」→ 把刚补进去的当日**又删掉**。
> 现在「当日剔除」的判断**只作用于日K 自身**（`in_session = k_last == today and now.hour < 15`），腾讯补进来的当日在去重后**追加**，不受该逻辑影响。
> 排查手法：看 `data.market_volume.date` 是否等于当日；不等就查 `fetch_tx_turnover_today()` 是否返回了值、以及 `_bk_fetch_ths_amount` 最新日期是否停在前一日。

### 3.13 消息速览（⛔ 前端卡片已删，数据仍在）
- ⛔ **必看页的「消息速览」卡片已于 2026-09-18 删除**（`renderNews()` 与对应 CSS 一并清除）——**别再往回加**。
- 但 **`data.news` 字段仍在产出**，用途只有一个：**给复盘文本（B 段）引用**。
- 口径：只显示**完整句**的简述（源站摘要常被截断 → `news_brief()` 按句号只取完整句、拼到 ≈72 字，全无完整句则回退标题）。字段 `news[k][].text`。**已移除 jieba 关键词**，不要再引入。
- 数据源＝**同花顺 7×24 快讯**（`news.10jqka.com.cn`，`THS.fetch_news(pagesize=200, pages=N)` → 按 `(ts,title)` 去重 → 既有关键词四分类）。

### 3.13b 涨停原因 / 事件标签
`THS.fetch_reason_index(dates, limit=60)` 跨日汇总 `block_top`：
`{code: {'concepts': [reason_type 拆出的题材词], 'info': reason_info 长文, 'date': 'YYYYMMDD'}}`。
- `reason_type` 形如 `光模块+半导体设备+人形机器人` → 按 `+` 拆成题材词；
- `reason_info` 形如 `行业原因：1、工信部…` → 供事件/消息标签匹配（重组 / 中标 / 业绩预增…）。
- 实测：2 天 → 96 只；12 天 → 398~423 只。这是**取代东财个股新闻搜索**的口径。

### 3.14 「必看」页 · 大盘宏观面板（`macro.py` → `data.macro`）

2026-09-18 改版：必看页**只保留置顶「沪深量能」**，其余三个旧卡片（盘后全景·指数与量能 / 连板晋级·昨日涨停表现 / 消息速览）
已删除；替换为 5 个模块，统一由 `renderMacro()` 渲染 **7 个容器**。
> ⚠️ 旧卡片对应的 JS（`renderIndex` / `renderBoardPerf` / `renderNews`）、`IDX_NAME` 常量与它们的 CSS 已一并清除，
> 不要因为「找不到」而重新加回来。

**取数：`python macro.py`**（4~8s；盘中/盘后都能跑，实时口径），产出 `data.json` / `data.js` 的 `macro` 键。

> ✅ **已与主流程一体（2026-09-18）**：`fetch_data.py` 末尾第 **[7/8]** 步**自动调用** `macro.run()`，
> 盘后跑 `fetch_data.py` 即顺带刷新 macro，不需要单独跑。跳过用 `python fetch_data.py --no-macro`。
> 调用被 `try/except` 包住 —— macro 失败**不影响**主体数据落盘。
> `refresh.bat` 同步为 3 步（fetch_data → enrich_tags → module4），macro 已含在第 1 步内。
> 前端若仍显示「暂无宏观数据（运行 python macro.py 生成）」，说明 `data.js` 里**没有 `macro` 键** —— 通常是：
> ① 只单独跑了 `fetch_data.py --no-macro`；② macro 抓取抛异常被吞；③ 浏览器缓存了旧 `data.js`（强制刷新 / 清 Service Worker）。

| 模块 | 容器 id | 数据来源 |
|---|---|---|
| ① 大盘概览与今日资金主线 | `mkt-indices` / `mkt-kpi` / `mkt-mainnote` | 同花顺 realhead（小盘宽基腾讯兜底）+ 涨停池 + 行业资金流 |
| ② 产业板块涨跌热力网格 | `heat-grid` | 同花顺 realhead（`SECTOR_WATCH`，**清单 28 条 → 实际渲染 27 格**）|
| ③ 主力资金行业净流入排行 | `fund-rank` | 同花顺行业资金流榜 |
| ④ 风格轮动 · **相对强弱（RS 折线，近20日）** | `style-box` + `style-note` | 宽基指数**日K自算 RS** |
| ⑤ **轮动研判 · 行业配置观点** | `notes-box` | 由 ①②③④ 的当日数据推导 |

**关键口径与坑**：
- **指数源**：同花顺 `realhead` 优先（取不到才用腾讯 `qt.gtimg.cn` 兜底，`src:'tx'`）。两源数值**已交叉验证一致**（上证50 2867.47、沪深300 4507.02、中证500 7799.74 三家同值）。
- 🔴 **大盘概览的指数行只有 4 个（2026-09-20 用户要求）**：**上证指数 / 深证成指 / 创业板指 / 沪深300**（白名单 = `macro.INDEX_SHOW`）。
  - ⛔ **已停止取数、不再统计**（用户点名删除）：**科创50 / 北证50 / 中证500 / 国证2000**。**别再往回加**。
  - ⚠️ **仍保留取数的只有 `上证50` / `中证1000`** —— 它们**不在概览展示**，但 ⑤ 轮动研判的「研判3·风格」
    依赖（`build_notes()` 里的 `value` / `small`），**从 `INDEX_SPEC` 删掉会让整条研判3不再产出**。
    所以 `macro.indices`（下发前端）是**过滤后的 4 条**，而 `build_notes()` 收的是**全量**。
- **两市成交额**：上证 `zs_1A0001` + 深证综指 `zs_399106` 的 realhead **`19` 字段**（元）相加 —— 盘中即为实时值，不必等收盘。
- 🆕 **KPI 行末尾「仓位建议」卡（2026-09-21 用户指定）**：`#mkt-kpi` 的第 8 个卡片（`.kpi.judge-empty`）。
  - **封板率 < 75% 且 跌停 > 10 → 「空仓」**（红底红字，风险警示）；否则 **「可参与」**（中性，不着色）。
  - ⚠️ **必须用 `macro.mood.zt_rate` 的原始比例**（如 `0.7549` —— 界面四舍五入成 75%）直接与 `0.75` 比。
    **先 `Math.round(zt_rate*100)` 再比会错**：`0.746`（显示 75%）会**漏判**、`0.7549` 会**误判**。跌停同理用原始家数 `mood.dt`。
  - 边界按**严格**不等号：封板率恰好 75%、跌停恰好 10 → **不触发**。
  - 两项数据任一缺失 → 显示 **「—」**（不给结论），**不要默认成「可参与」**（宁可无结论也不要错结论）。
  - 「可参与」**故意不配绿色** —— KPI 行既有语义是「绿＝跌」，配绿会混淆。只让触发态抢眼。
  - **纯前端判定**（`mood.zt_rate` / `mood.dt` 前端本来就有），后端与取数层**不需要改**。
- **产业板块资金匹配必须「行业榜 → 概念榜」双路**：CPO / 存储 / 机器人 / 核聚变 / 数据中心 等属**概念**，不在行业榜里。
  只查行业榜时净额仅覆盖 12/27；双路后 **27/27**。
- **简称与全称对不上要加别名**：`macro.SECTOR_ALIAS`（`CPO → 共封装光学(CPO)`、`存储 → 存储芯片`）。
  原因：同花顺把英文缩写放在**括号内**，而 `_stem_name()` 会把括号内容剥掉 → 纯简称永远匹配失败。
- **热力配色** `heatStyle(p)`：涨红跌绿 × **6 档深浅**（阈值 0.5/1/2/3.5/6%），深档用白字保证对比度。
- **属性值转义用 `escA()`**（`esc()` 只处理 `&<>`，不转义双引号）—— `data-tip` / `title` 里会拼领涨股名，直接用 `esc()` 有破版风险。
- ⚠️ **浮层必须自绘，不能用原生 `title`**：**手机端没有 hover，原生 `title` 在触屏上完全不显示**
  （曾因此导致"手机上点击看不到领涨股"）。现用 `#tip-layer` 自绘浮层：
  · 数据以 `data-tip-title` + `data-tip`（多行用 `\n` 分隔，行内 `键：值` 形式）挂在元素上；
  · 事件**委托在 `document`** 上（`mouseover`/`mouseout`/`click` 捕获阶段）→ `renderMacro` 重绘后**无需重新绑定**；
  · 触屏：点一下显示、**再点同一元素收起**、点别处收起、滚动/缩放收起、**3.5s 自动收起**；
  · 桌面：保留 `mouseenter`/`mouseleave` hover（用 `'ontouchstart' in window` 区分，触屏设备不绑 hover 以免和 click 打架）；
  · `tipHTML()` 把 `键：值` 渲染成两列，数值按涨红跌绿着色；定位时上方放不下自动翻到下方，水平方向夹在视口内。
  · ⚠️ **定位必须在 `requestAnimationFrame` 里再校正一次**：刚写 `innerHTML` 的同一帧
    `offsetWidth/offsetHeight` 可能为 **0** → 会算出负 `left`、浮层贴左边缘甚至被裁。
    现抽出 `place()`，先算一次 + rAF 再算一次；尺寸拿不到时退到「视口内最小锚定」。
  · 已挂载：热力网格 **27 格** / 资金排行 **20 行**（含净流出部分）。
    ⛔ **资金主线那 6 个卡条已删**（见下条），不要再给它挂 `data-tip`。新增卡片要展示细节时**沿用这套**，别再写 `title`。
- **「行业配置研判」的定位**：全部由当日客观数据推出（资金集中度 = 前 3 净流入 / 全部净流入为正者之和；
  量价背离 = 涨幅 >0.5% 但净流出 <-1 亿；风格取向 = 创业板指 − 上证50），**仍是客观描述，不含涨跌预测**（见 §4 第 1 条）。

**④ 风格轮动 · 相对强弱（RS）—— 2026-09-18 改版**
- **取数**：`macro.build_style_rs()` 用 `THS.fetch_daily_kline(zs_xxx, n=RS_DAYS+12, prefix='')` 取**指数日K**
  （`STYLE_RS` 常量：大盘价值 `zs_399300` ↔ 小盘成长 `zs_1B0852`；周期 `zs_399998` ↔ 消费 `zs_399396`）。
- **RS 构造**：两侧各以**区间首日收盘**归一化 → 求比值（右/左）→ 再整体归一化到 **100 为基**。**上行＝右侧（成长/消费）相对走强**。
- **✖ 不能用 realhead 快照**：快照只有当日涨跌幅，**画不出 20 日走势**（旧版 `m.style` 双向条形用的是 realhead，已弃用于本卡）。
- **字段**：`{title, left, right, left_tag, right_tag, labels[], series[], tail, trend:{dir,days,delta}}`；
  `left/right`＝**维度名**（如「大盘价值」「小盘成长」），`left_tag/right_tag`＝**强弱名**（如「价值强」「成长强」）——
  两者别混用：卡头显示维度名，图上下轴标注强弱名。
- **配色**：**右侧占优 → 红**、**左侧占优 → 绿**（把「涨红跌绿」惯例落到「谁占优」上，A 股口径）。
- **前端**：纯**内联 SVG**（`rsSvg(series, color, idx)`，viewBox 240×62 + `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"`），
  **不依赖 ECharts**；100 基期画一条虚线参考线；宽高自适应容器（手机端 `height:56px`）。
- **配文** `style_rs_note`：由 `build_style_conclusion()` 生成（方向 + 连续天数 + 天平结论），**不含预测**。

**⑤ 轮动研判 · 行业配置观点 —— 2026-09-18 改版**
- 每条输出结构（与前端 `.cfg` 卡片一一对应）：
  `tag/level`（**超配 / 标配 / 观察**）· `init`（字母角标）· `title` · `body`（必带数据）· `drivers[]` · `risks[]` · `data`。
- 前端按 `level` 映射左边框色：`超配 → 红`、`标配 → 橙`、`观察 → 灰`（`.lv-over / .lv-neu / .lv-obs`）；
  tag 行：`驱动` 绿底、`风险` 红底。
- ⚠️ **第 2 条「量价背离」是用户指定必须保留的**，与「资金集中度」「风格取向」合并 = **共 3 条**。
- 沿用 §3.13 的**客观口径**，评级词只反映当日数据的集中度/强度，**不是投资建议**。

**① 今日资金主线 —— 一句话概览（2026-09-18）**
- ⚠️ **原本的 TOP6 卡条（`.mkt-main` / `.ml-item`）已按用户要求删除**（2026-09-18 晚）——
  只保留标题「今日资金主线」+ 概览条。**别再往回加**；`macro.mainline` 字段仍保留（后端在用），
  但前端不再渲染卡条。
- **概览** `#mkt-mainnote`（橙色提示条）：数据来自 `macro.mainline_note`，由 `build_mainline_note()` 产出
  `{lead, dirs[], focus, total_yi, top_name, top_yi, judge}`：
  · `dirs` 由 `MAINLINE_SECTOR_GROUPS` 把同花顺行业名归到短线口径的产业方向
    （AI算力 / 算力租赁 / 高端制造 / 新能源 / 医药生物 / 大消费 / 金融地产 / 周期资源），未命中原样输出；
  · `judge` 由「净流入合计 + 科技成长占比」推出，**客观描述，不含预测**。
- 概览条为卡片末行（`.ml-note` 底距 0）；`.mkt-kpi` 底距 13px、`.mkt-main-h` 底距 6px。

### 3.15 「板块」页 · 高标跟踪（`bigboards.py` → `data.bigboards`，2026-09-21 用户指定）

> 🔴 **本章有三次改动，务必按最后一次为准（2026-09-21 晚）：**
> 1. 初版：跟踪 ≥4 连板 → 按题材归类 → 跌停标出 → 每行末尾**删除按钮**（点它删票）。
> 2. 第二次：**删除按钮与整套删除机制已删**；「两市成交额」卡已删。
> 3. **【当前】**用户要求**跌停票（「不再跟踪」）不再显示**（「你不跟踪的就不用显示了，要么太多了」）——
>    卡片里**只渲染 `status='tracking'` 的票**；⚠️ 但**数据层仍是全量**（跌停票仍在 `data.bigboards.groups` 里）。
> **「不再跟踪」≠「永久排除」（用户原话纠正，核心口径）** —— 见下 §3.15.0。

#### 3.15.0 🔴 「不再跟踪」的准确含义（最容易被误读的一条）

**用户原话（2026-09-21 纠正）**：
> 「反正不跟踪的票已永久排除，这个是错的，**不是让你永久排除，只是不在跟踪了，懂吗？别的地方统计数还是要用的**」

| | 正确理解 ✅ | 错误理解 ⛔ |
|---|---|---|
| 跌停票（`status='limit_down'`） | **不再跟踪**、**卡片里不显示**（2026-09-21 晚更新）；但**仍留在数据层** `data.bigboards.groups` 里（`status='limit_down'` 保留） | 把它从 `data.json` 里抹掉（=「永久排除」） |
| 对**其他模块**的影响 | **零影响**：连板梯队、板块涨停家数、复盘文本、节点池……**一律照旧使用**全量数据 | 把它从涨停池 / 历史里剔除，导致别处统计数变小 |
| 显示层 vs 数据层 | **显示层过滤**（`renderBigboards` 里 `filter(s => s.status !== 'limit_down')`），**数据层不动** | 把过滤逻辑写进后端 `build_bigboards()`，让 `data.bigboards` 本身变少 |
| 要不要「黑名单」文件 | **完全不需要**。已废弃：`bigboards_hidden.json` 与 `hide_bigboard.py` 均已 `git rm` | 又去搞「跨设备全栈删除」 |

**一句话**：高标跟踪卡片是一张**「仍在跟踪中」的清单**（跌停的收起来不显示），
但跌停票作为**历史事实**必须留在数据里供全站使用。**绝不产生任何跨模块删除副作用。**

> ⚠️ **判据 / 回归防线**：`_shot.js` 有一条断言专门盯这个 ——
> 「数据层仍为全量（含跌停票）：groups 内 N 只 / 其中跌停 M == stats.total」。
> 一旦有人把「不显示」误改成「后端剔除」，这条会立刻红。

#### 3.15.1 需求与位置

**需求原文（初版）**：跟踪统计近半个月内 **≥4 连板**的股票 → 按**当前最贴合的题材**归类 → 持续跟踪 → 该票**跌停即标出并停止跟踪**。
（初版还有「每行末尾一个删除按钮」—— **该按钮已删，不要加回来**。）

**位置**：`panel-boards` 面板**第一张卡片**（`#bigboards-card`），在「产业板块·3日 TOP10」之上。
渲染入口 `renderBigboards()`，由 `renderBoards()` 开头调用（`renderBoards` 是板块面板的唯一重绘入口）。

#### 3.15.2 后端模块 `bigboards.py`

- 常量：`BIG_DAYS = 10`（**≈ 近半个月 = 10 个交易日**，与 `NODE_KEEP_DAYS` 同口径）、`BIG_MIN_LBC = 4`。
  ⛔ **`HIDDEN_FILE` 常量与 `load_hidden()` / `save_hidden()` 已整体删除**（随黑名单机制一起废弃），**不要再加回来**。
- 入口 `build_bigboards(zt_hist, days, dt_hist, theme_of=None, now=None)` —— ⚠️ **没有 `hidden` 参数了**（已去掉）：
  ① 扫窗口 `days[-BIG_DAYS:]` 内的 `zt_pool`，逐票收集**连板轨迹**（天数 → lbc）；
  ② 过滤 `max_lbc >= BIG_MIN_LBC`；
  ③ 判定 `status`：`tracking`（仍在跟踪）/ `limit_down`（已跌停，台账上盖个章 —— **票仍留在列表里**）；
  ④ `cur_lbc`：只有窗口末日仍在涨停才算当前连板，否则给 `None`（前端只用 `max_lbc`）。
- ⚠️ **跌停必须发生在「最后一次涨停之后」才判为 `limit_down`**：否则一只票先跌停、后连板（反包），
  会被错误地标成「已跌停 + 停止跟踪」，而它其实正当红。判据 = `最后跌停日 > 最后涨停日`。
- ⚠️ **概念 / 行业取「最后一次涨停那天」的值**（`win` 升序覆盖赋值）——
  取窗口首日会拿到过时题材（一只票可能中途换主线）。
- 排序：组内 **有活口（tracking）优先 → 最高板降序**；组间 **有活口优先 → 该组最高板 → 票数**。

#### 3.15.3 题材归类 `_theme_of(code, market, fb, ind)`（在 `fetch_data.py`，4 步优先级）

1. **涨停原因**（同花顺「今天为什么涨停」标注的板块名）——**按位置**取第一个命中项。
   顺序可靠，**不能用"当日最热"来选**（原因见下）；
2. **F10 题材**——**位置不可靠**（一票挂 5~6 个概念、顺序含泛词），故改按**当日涨幅**取最热的命中板块；
3. 只剩 `WEAK_THEME_KW`（次新 / 国企改革 / 破净 / 贬值受益 … 风格属性类弱词）→ 降权使用；
4. 一个在榜板块都没命中 → 退回原始词，**不要塞进「其他」**。
- ⚠️ **归类键必须是「命中的板块名」，不能是「票自己的概念名」**：用后者时 11 只票 → 11 个组（完全不收敛）；
  只有落到**同一份板块榜的名称**上才会分组。
- ⚠️ **命中的板块名靠 `match_board(cname)` 分档解析**（精确 > 去后缀相等 > 模糊包含，同档取涨幅最高）——
  不分档会让泛词「AI」被「智谱AI」之类的具体板块抢走。
- ⚠️ **反例留证**：爱仕达 F10 挂了 5~6 个泛概念，按"当日最热"会归到**土地流转**（无关）；
  改「涨停原因优先」后归到 **人形机器人**（正确）。→ 踩坑记录见 §9。

#### 3.15.4 前端 = 纯展示（无任何交互/删除）

- ⛔ **已删、不得恢复**：`BB_DEL_KEY` / `bbDeleted()` / `bbAddDeleted()` / document 级 click 委托 /
  行内 `<button class="bb-del" data-bb-del>` / `.bb-del` `.bb-del:hover` `.bb-cur` `.bb-dropped` CSS /
  `localStorage['bb_deleted_v1']`。**`index.html` 文件头已加 ⛔ 注释锁死这条。**
- 🔴 **显示层过滤（2026-09-21 晚新增）**：`renderBigboards()` 开头把每组 `stocks` 过滤成
  `s.status !== 'limit_down'`，并丢掉过滤后为空的组（`.filter(g => g.stocks.length)`）。
  → **跌停票（「不再跟踪」）不显示**，卡片只留跟踪中的活口。
  ⚠️ **这是纯显示层过滤 —— 绝不改后端 `build_bigboards()`**；`data.bigboards.groups` 仍是全量。
- `renderBigboards()` 只做三件事：① 填 `#bb-note`（「近 N 个交易日」）② 填 `#bb-sum` 汇总条
  ③ 用**过滤后的** `groups` 铺行。
  **汇总条格式**（与显示一致）：`MM-DD ~ MM-DD · 跟踪中 <b>N</b> 只`，若 `stats.limit_down > 0` 再补
  `（另有 M 只已跌停，不再跟踪）`。⛔ **不再报「共 N 只」**（那是含跌停票的总数，与卡片实际显示不符）。
- **空态**：过滤后一个活口都没有 → `暂无跟踪中的高标（近 N 个交易日无 ≥M 连板活口）`。
- **视觉语义**：红＝强（`6板` 红字）、蓝＝进行中（`跟踪中` 蓝底徽章）。
  ⚠️ 因为跌停票不再显示，`.bb-row.ld` / `.bb-badge.ld` 这套「跌停绿底」样式**在当前数据下不会出现**
  （CSS 暂留，供将来若恢复显示时用；但**不要为此恢复显示**，用户明确要求不显示）。
  **其余高亮一律不得用红**（沿用 §3.14 的配色纪律）。
- `esc()` 转义所有来自数据的文本；浮层细节沿用 `#tip-layer` 自绘（**不能用原生 `title`**，见 §3.14 触屏坑）。

#### 3.15.5 验证与当前实测

**`_shot.js` 断言（2026-09-21 晚更新，全部恒真 —— 谁把「减法」或「只不显示」搞错就立刻红）**：
卡片存在 / **无删除按钮**（`[data-bb-del]` 计数 === 0）/ **无 `.bb-cur`** /
**`bbDeleted` 与 `bbAddDeleted` 均 `undefined`** / **卡片里 0 行跌停票**（`.bb-row.ld` === 0）/
**实际渲染行数 == `stats.tracking`**（不再是 `stats.total`）/ 汇总条报「跟踪中 N 只」且 == `stats.tracking` /
汇总条附注「另有 M 只已跌停」（M = `stats.limit_down`，M=0 时不出现）/ 汇总条**不再**报「共 N 只」/
`localStorage` 无任何 bb 相关键 /
🔴 **数据层仍为全量**（`D.bigboards.groups` 内票数 == `stats.total`，且其中 `limit_down` 数 == `stats.limit_down`）
—— 这条专门防「把『不显示』误改成『后端剔除』」。

**当前实测（2026-09-21 晚）**：近 10 日 ≥4 板共 **12 只**（跟踪中 6 · 跌停 6）。
**卡片显示 6 只 / 5 个题材组**（风电 闽东电力·锡华科技 / PCB 澳弘电子 / MLCC概念 华瓷股份 / 房地产 世联行 / 教育 内蒙新华）；
跌停 6 只（零售 百大集团 / 玉米 亚盛集团 / 人形机器人 爱仕达 / 旅游概念 桂林旅游 / 智能家居 瑞尔特 / 网络安全 中新赛克）
**仅在汇总条以计数提示，不占版面**。

### 3.16 ⚠️ 交易日源「盘后滞后」——`fetch_trade_days()` 的当日校验（2026-09-21 修的真实 bug）

**症状**：盘后（15:00 后）重跑 `fetch_data.py`，`data.json` 的 `date` **回退到上一个交易日**（如 09-21 跑出 09-18），
整条管线把**今天的涨停池数据挂到了昨天的日期上**。

**根因**：`fetch_trade_days()` 的两个源**盘后都不含当日** ——
主源腾讯日K `qt.gtimg.cn`、备源同花顺指数日K `THS.fetch_daily_kline`，收盘后一段时间内返回的最后一根 K 线仍是**昨天**。
**「源成功但数据陈旧」比「源全失败」更危险**：源全失败会走**缓存兜底**（缓存里反而有今天），
源成功但陈旧会**绕过兜底**，于是 `days[-1]` 是昨天 → `today = days[-1]` 全盘错位。

**修复**（两道，缺一不可）：
1. `_confirm_today()`：用**涨停池**独立确认「今天是不是已经收盘的交易日」——
   `weekday() < 5 and now.hour >= 15` 且 `THS.fetch_zt_pool(today)` 非空 → 返回 `'YYYY-MM-DD'`；否则 `None`。
   （涨停池是**当日实时产出**，不受日K滞后影响，是最可靠的存在性证据。用 `try/except` 包住，失败即返回 `None`。）
2. `fetch_trade_days()` 拿到列表后校验：`ds = _confirm_today(); if ds and got[-1] < ds: got = got + [ds]`，
   并打印 `[warn] 交易日源缺当日 … （指数日K盘后滞后，非接口失败）→ 已确认今日为交易日并补上`。

**排查口诀**：盘后看到 `date` 不对，**先看日志有没有这条 warn**；
再看 `data.json` 的 `market_volume.date` 是否 == 顶层 `date`（两者应恒等，且都应等于当天）。
另注意：`fetch_data.py` 末尾会**自动调 `macro.run()`**，所以一次重跑会连带刷新必看页宏观面板。

**教训（已并入 §9.2 第 13 条）**：「把日K的最后一根当成今天」是一个**独立于数据源**的假设；
只要上游在收盘后短暂滞后就会全线错位。**任何"以最新可获取数据推断当前日期"的写法都要配一个独立的存在性校验。**



---

## 4 · 硬性规则（不可违反）

1. **复盘零主观预测**：只做客观数据整理与罗列，**严禁**"明天会涨 / 大概率走强 / 建议买入"这类预测或建议性表述（"九、连板推演"只写"条件是否满足"）。
2. **复盘内容一次性完整文本输出**，不拆分、不分多次输出。归档只写**单一滚动文件** `A股复盘记录.md`（最新在最前，保留最近 30 个交易日）。
3. **每只个股必须精准标注当下市场核心炒作题材概念**，命名贴合主流短线认知（如「稳定币/RWA」「猪肉养殖」「中船系」），不用笼统行业分类糊弄。
4. **股票池不含科创板、ST 股、北交所**（见 3.1）。

---

## 5 · 工程纪律

- **数据源＝同花顺（2026-09-17 用户定，全站已切换完毕）**：东财仅剩 **2 处**不可替代的缺口（见下）。
  · **统一取数层 = `ths_source.py`**（约 1150 行）。新增任何取数功能，**先在这里加函数**，不要在 `fetch_data.py` 里散写 URL。
  · 已迁同花顺的：交易日、涨停池+情绪、炸板池、连板梯队、分时、实时快照、板块日/周K、行业榜、概念榜、资金流榜、3日/5日涨幅、指数快照、7×24 快讯、F10 题材/地域、板块成分股统计、涨停原因、昨日涨停表现（自算）。
  · ⚠️ **东财缺口 ①：跌停个股池明细** `push2ex.eastmoney.com/getTopicDTPool`（同花顺无公开接口，只有跌停家数）。
    该域不在被封的 `push2his` 路径下，实测可用；若将来不可用，前端「跌停板梯队」降级为只有家数、无个股明细。
    ⚠️ 踩坑：跌停池用 `getTopicDTPool`，但 **`dpt` 仍是 `wz.ztzt`**（写成 `wz.dtzt` 会返回 0 条）。
  · ⚠️ **东财缺口 ②（2026-09-20 新增）：板块成分股 → 涨停家数** `fetch_data.em_board_zt_count()`
    （`push2.eastmoney.com/api/qt/clist/get?fs=b:BKxxxx`）。
    **为什么**：同花顺板块成分股页已被 **chameleon JS 反爬拦死**（见 §8.1），`THS.count_board_zt()` 恒返回 None，
    导致板块榜「涨停/全部」的涨停家数全空。东财 push2 与缺口①同属**未被封的域**（不是 `push2his`），
    且返回自带 `f3` 涨跌幅 → 配 `_is_limit_up()` 即得涨停家数。**同花顺那路保留为退路**，接口恢复即自动生效。
    ⚠️ 踩坑：东财 `pz` **实测封顶 100**（传 500 也只回 100）→ 按「返回条数 < pz 即尾页」判停会**只取到第 1 页**。
  · **iFinD MCP** 与网页接口同源同值，可用于会话内校验，但 **MCP 进不了定时脚本**（见 3.9.2）。
- **同花顺 `realhead` 字段码**（`d.10jqka.com.cn/v6/realhead/bk_<码>/last.js`）：
  `10`=最新价、`264648`=涨跌额、`199112`=涨跌幅(%)、`19`=成交额(元)、`8/9`=最高/最低、
  `37`=成分股总数、`38`=上涨家数、`39`=下跌家数、`13/14/15/527198`=资金流。
  ⚠️ **`7` 对个股是昨收、对指数/板块是开盘价** → 昨收统一用「现价 − 涨跌额」，不要直接读 `7`。
- ⚠️ **同花顺涨停池 `limit` 硬上限 200**（`>200` 返回 `status_code=-1`）。
- 🔴 **连板数（`lbc`）的正确来源 = 同花顺「连板梯队」接口 `continuous_limit_up`**
  （`THS.fetch_lianban_ladder(date)` → `[{height, stocks:[{code,name,market}]}]`）。
  **不要**从涨停池的 `high_days` / `high_days_value` 自己推 —— 详见 §9.2 第 12 条的事故说明。
  `high_days_value` 编码实测为 `(板数 M << 16) | 天数 N`（对应页面上的「N天M板」），
  **M 是 N 天内的涨停次数、含中间断板，N ≠ M 时就不是连板数**。
  - **口径定义（用户 2026-09-21 明确）**：「连板」= 自本轮**第一个涨停**起，之后**每个交易日都必须涨停**，
    **只要断开就不算连板**。断板后重新涨停 → 连板数从 **1** 重新起算。
  - 该接口不返回 `height < 2` 的组 ⇒ **「不在梯队里」＝当日涨停不属于连续段**（首板 / 断板反包）⇒ 记 **1**。
  - **`lbc` 是全站唯一连板数来源**：涨停池 → `build_ladder`（连板高度梯队）→ `track_status`
    → **节点票池 `stocks[].boards`** → **`build_candidates` 的 `trigger` / `stocks` 两条分支**
    → 前端「连板候选池」的 `N板`。**改口径只需改这一处**。
  - 🛡️ **两道保险**（`fetch_data.recount_lbc_by_continuity()`，在 zt_hist 构建后、节点构建前执行）：
    ① **交叉校验**：用历史涨停池按「严格连续」自算，与权威接口值比对，不符即告警（暴露接口/解析回归）；
    ② **降级修正**：接口偶发无返回时 `fetch_zt_pool` 记 1 并标 `lbc_src='fallback'`，
    自算层用历史池把真值还原（**故意不退回 `high_days` 的 M** —— 那是含断板的错误口径）。
    ⚠️ 自算跳过「数据缺失的交易日」（缺失 ≠ 未涨停）；窗口（默认 20 日）内一路连到边界时只算下界，不覆盖权威值。
  - ⚠️ **`_parse_high_days()` 已改名 `_m_boards_from_high_days()`**，语义明确为「N天内涨停次数 M，**不是连板数**」。
    炸板池原本用它的值填 `lbc` 字段（**静默的口径陷阱**）→ 已改名为 `m_boards` 并去掉 `lbc`（该字段本无消费方）。
- ⚠️ 前端顶层的 `let/const`（如 `bkCode`、`THS_BK_MAP`）**不会挂到全局对象**上 → 从外部赋值无效，测弹层要走真实入口（如 `openBoard(code,name,pct)`）。
- **改前端后必须三步验证**：① 抽出内联 JS → `node --check`；② 桩件模拟 DOM 跑 `init()` 与各 `render*()`；③ 提交后实测线上 Pages。

- **改取数逻辑改在一处**：板块/指数 K线统一走 `board_kline_src.py`（`fetch_data.py` 与 `fill_board_kline.py` 共用），**别在两处各写一套**。
- 🔴 **唯一外部依赖：`pypinyin`（股票缩写 / 拼音首字母）**
  - 位置：`fetch_data.py` 顶部 `from pypinyin import lazy_pinyin` → `pinyin_abbr(name)`。
  - ⛔ **绝不允许"缺了就静默返回空串"**：2026-09-22 事故＝环境重建后 pypinyin 没装回来，
    **全站「股票缩写」全部变成空字符串、脚本一声不响跑完**，直到用户发现「缩写怎么又丢了」。
    现已改为：`PINYIN_OK` 标志 + 开头大字告警 + 结尾覆盖率自检（见 §9.2 第 17 条）。
  - ⚠️ **本机清华 pypi 镜像不通**（返回 `No matching distribution found`）→ 必须用**官方源**：
    `<你的python> -m pip install pypinyin`。`setup.bat` / `refresh.bat` 已改为「官方源优先 + 启动自检」。
  - 装完**必须重跑 `fetch_data.py` + `enrich_tags.py`** 才补得回来。
    ⚠️ `candidates` / `recommend` 是从盘前版本**原样沿用**的（不重算）→ 已在 fetch_data.py 的沿用处加
    **「派生字段自愈」**（`pinyin` 是名字的纯函数，沿用即重算），否则这两块的空串永远修不掉。
  - 自检：每次跑完打印 `股票缩写(pinyin)：N/M 非空`（正常 > 90%）；`_shot.js` 亦有断言（< 90% 直接红）。
- **删文件前必须全项目 grep 验证引用**（曾误判 `review_data.json` 为死文件，它其实是 `fetch_data.py` 兜底读取的活文件）。
- **同一函数有多个出口时要一起改**（`build_candidates` 有两处 `append`，漏改第二处导致 `zbc` 静默丢失）。
- **所有网络抓取一律并发**（`ThreadPoolExecutor`）；`http_get` 超时 **8s**、退避 **0.6+0.6i**（原 15s 超时，网络抖动时单请求可阻塞 45s）。候选竞价量因接口易限流**有意保持串行**。
- `echarts.min.js` **本地自托管**，不用 CDN。
- 用 Python 批量替换文本后**立刻** `py_compile` / `node --check`（曾因 `\n` 写成 `/n` 产生语法错误）。
- 用 `Edit` 工具改长行文本时，`old_string` 必须与文件**完全一致**（含缩进）。
- 🔴 **临时文件必须放系统临时目录，不要留在项目根目录**：
  15:35 的「GitHub 每日备份同步」任务是 `git add -A` —— **任何没被 `.gitignore` 忽略的临时文件都会被提交进公开仓库**。
  `.gitignore` 只忽略了 `_*.py` / `_*.json` / `_*.html`，**`_*.js` 不能整类忽略**（`_shot.js`、`_smoke_init.js` 是正式文件）。
  2026-09-22 事故：临时备份 `_bak_data.js`（data.js 的副本，500KB）被备份任务连带提交进公开仓库
  → 已在 `.gitignore` 补 `_bak_*` / `_tmp_*` / `_*_bak.*`，但**治本仍是"别在项目里放临时文件"**。
- **推送 `github.com` 常抖动**（502 / 000 / `Empty reply from server`），需**轮询重试**（先 `curl` 探活再 push，循环 10~20 次）；沙箱对东财也时通时断，与用户浏览器无关。
  **若轮询仍不通**（`github.com` 整段不可达、git 协议被拦，但 `api.github.com` 可达）→ 改用 **`python push_api.py`** 走 GitHub API 完成等效推送：
  它以远端 main 为父提交上传本地整棵树（blob 取自 git 对象库，规避 CRLF 假差异），并在本地重建同一提交对象后 `reset --soft`，
  **使本地 HEAD 与远端 SHA 完全一致、不留分叉**；无变更时自动跳过。

---

## 6 · 换电脑后：必须重建的自动化

自动化任务存在 WorkBuddy 系统里，**不随文件夹走**，换电脑会丢。用 `automation_update` 重建下面 **3 条**（工作目录统一填 `D:\软件\WorkBuddytwo\神秘力量\stock_dashboard`）。

> ⛔ **曾经有 4 条**；第 1 条「盘前新闻刷新 9:10」**已于 2026-09-18 删除，不要重建**（见 §2）。

| # | 名称 | 频率 | 内容 |
|---|---|---|---|
| 1 | 盘前刷新（连板候选池 + 竞价判定）| 周一至周五 09:26 | `python premarket.py`；校验 `candidates>0`、`auction_is_live=true`；再 `workbuddy_sites_deploy` 发布 |
| 2 | 复盘 + 仪表盘刷新 | 周一至周五 15:05 | `fetch_data.py` → `enrich_tags.py` → `module4.py` → 发布 → 复盘文本归档（`save_review.py --keep 30`）；**不得重算候选池与推荐** |
| 3 | 仪表盘 · GitHub 每日备份同步 | 周一至周五 15:35 | 纯 `git add -A && commit && push`，不跑抓取、不改代码 |

**当前实际存在的任务 ID**（2026-09-20 核对，便于排查）：

| 名称 | ID | 频率 |
|---|---|---|
| 盘前刷新（连板候选池 + 竞价判定）| `bce8ac58-2380-47ac-90fc-bac1f068d0aa` | 周一至周五 9:26 |
| 复盘 + 仪表盘刷新 | `74fffb7e-7418-4037-8756-35c7135a635d` | 周一至周五 15:05 |
| 仪表盘 · GitHub 每日备份同步 | `0c9dba20-b812-489b-ab57-b0a25fe7ae72` | 周一至周五 15:35 |

> 说明：`46a00d70`（C盘缓存每日清理 9:30）与另外 3 条社区类任务**不属于本项目**，不要动。

### 6.1 三条任务的提示词里**必须保留**的一段（会话环境）

自动化跑在**无界面（无人值守）会话**里：`present_files`（弹文件卡片 / 打开预览）等 UI 工具**未注册**。
如果不写清楚，代理会去调用它、然后在**每天的汇报里多写一句**
「本会话 `present_files` 工具未注册，无法弹出文件卡片，故以绝对路径代替」——用户每天收到这条噪音，
会来问「每天的刷新总报这个是什么情况」（2026-09-22 实际发生）。

所以三条任务的提示词里都加了这段，**重建自动化时务必照抄**：

```
【会话环境·重要】本任务在无界面（无人值守）会话里运行：
  · `present_files` 这类"弹文件卡片 / 打开预览 / 展示产物"的 UI 工具未注册属正常现象。
  · 不要调用它；也不要在汇报/复盘里写「工具未注册 / 无法弹出文件卡片 / 无法展示 / 故以路径代替」
    这类环境说明 —— 用户每天都会收到这条汇报，纯属噪音。
  · 要交付结果：线上直接给永久链接 https://JIANWANGSAN.github.io/stock-dashboard/ ；
    本地文件直接给路径或正文。不必解释为什么给的是路径/链接。
```

同理：`workbuddy_sites_deploy` 若不可用，**只写一句**「今日未重新发布」，不展开解释工具情况。

---

## 7 · 交接话术（换电脑 / 换会话后复制给新 AI）

> 这是我做的 **A股短线复盘仪表盘**，代码在 `stock_dashboard` 文件夹，已备份到 GitHub 公开仓库 https://github.com/JIANWANGSAN/stock-dashboard
>
> 请按顺序做：
> 1. `git clone` 仓库（或直接拷文件夹），**先读仓库根目录的 `项目约定.md`** —— 取数规则、业务口径、工程纪律、**已知的删除项**全在里面，务必遵守。
> 2. 再读 `README.md` 的「一、换电脑怎么跑起来」，双击 `setup.bat` → `refresh.bat` 跑通。
> 3. 按 `项目约定.md` 第 6 节，用 `automation_update` **重建 3 条自动化任务**（不是 4 条，9:10 那条已删）。
> 4. `项目约定.md` 第 3 节的核心口径**不要改动**，第 9 节的「已删清单」**不要恢复**，除非我明确要求。
>
> ⚠️ **重要**：本文档与实际代码可能有时差。**动手前先 `grep` 核对代码现状**，不要凭文档描述就改；
> 更不要"照着描述把已删的东西加回来"——文档里带 ⛔ 标记的就是刻意删掉的。
>
> 注意：`github.com` 推送常抖动，需轮询重试（或改用 `python push_api.py`）；盘中不要跑全量 `fetch_data.py`。

---

## 8 · 已知遗留 / 注意事项

- `.workbuddy/` 与各类运行缓存（`.seal_cache.json`、`.stock_cache.json`、`.module4_kline_cache.json`、`.mx_members_cache.json`、`review_dump.json`）已在 `.gitignore`，**不入库**——所以**只有本文件是持久的**，重要结论请写进本文件，不要只依赖本地 memory。
- `_tags_dump.py`：**已在仓库中，是给涨停/炸板/跌停池补概念标签的正式工具**——
  15:05 任务的提示词已明确「**可直接复用，不要重复创建**」。⚠️ 别再把它当临时脚本删掉又重写。
- 🆕 `bigboards.py`：**高标跟踪的正式后端模块**（2026-09-21 新增，见 §3.15）。
  `build_bigboards(zt_hist, days, dt_hist, theme_of=None, now=None)` 由 `fetch_data.py` 主流程调用，
  产出 `data.bigboards`。**纯统计，无任何删除/黑名单副作用。不要当临时脚本删掉。**
  ⛔ 原 `load_hidden()` / `save_hidden()` / `HIDDEN_FILE` **已删**（2026-09-21 用户否定「永久排除」需求）。
- ⛔ `hide_bigboard.py`：**已 `git rm`（2026-09-21）**。曾是高标跟踪黑名单维护工具。**不要再创建。**
- ⛔ `bigboards_hidden.json`：**已 `git rm`（2026-09-21）**。曾是后端删除黑名单。**不要再创建。**
  「不再跟踪」只是展示层的事，不需要跨设备黑名单（详见 §3.15.0 / §9.1 #19#20）。
- `fill_mx_cache.py`：与「A0 补妙想板块成分股缓存」相关，被复盘记录引用，**暂不要删**。
  ⚠️ 但**妙想（mx_）取数链路已废弃**：新代码不要再调 `mx_` 接口补板块成分股，统一走 `ths_source.py`。
- `review_data.json`：**是活的**（`fetch_data.py` 在实时接口失败时兜底读取，`collect_review.py` 写入），**不要删**。
- 板块累计榜在 2026-09-15 口径切换时清空过 `board_history.json`，当时用东财 5 日涨幅顶替，约 3 个交易日后恢复正常累计。
- **2026-09-17 全站切同花顺后的已知缺口**：
  - `BK1652 新消费`：同花顺无对应板块 → 前端显示纯文本（不可点、无K线）；`THS_MISSING_FALLBACK_EM=True` 时可用东财单条补K线。
  - `885927 CRO概念`：同花顺无 `gn/detail` 概念页 → 成分股仍可按指数码取（成分股统计/涨停家数正常），但概念榜无此条。
  - **跌停个股池明细**：东财缺口 ①（见 §5）。
  - **板块成分股（涨停家数）**：东财缺口 ②（2026-09-20 新增，见 §5 / §8.1）。
  - `d3`/`d5` 由**同花顺板块指数日K自算**（榜页无此两列），与东财官方 f127/f109 口径**不完全等价**（指数基期不同、自算按收盘价累计）。
  - `stock_dashboard` 的 `board_kline` 仍以**东财 BK 码**为键（前端 `THS_BK_MAP` 做桥接），未改成同花顺码 —— 因为前端 `bklink` 与本地键都依赖它，改动面大收益小。
- `ths_source.py` 里的**探测脚本**（`_probe*.py`、`_scan_ths.py`、`_map_ths*.py`）是取数时的临时验证工具，**可随时删除**（不影响主流程）。

### 8.1 · 🔴 同花顺「板块成分股页」已被反爬拦死（2026-09-20 实测，重要）

- **症状**：`q.10jqka.com.cn/{thshy,gn}/detail/field/199112/order/desc/page/N/ajax/1/code/<码>/`
  **全部变体返回 401**，响应体是 `s.thsi.cn/js/chameleon/chameleon.1.7.min.*.js` 的 **JS 挑战脚本**。
  带 `Session` / `UA` / `Referer` / `X-Requested-With` 均无效；裸请求返回 200 但**空 body**。
- **不是网络问题**：同域主站 `q.10jqka.com.cn/` 首页 / 行业列表页 **200 正常**（52KB / 62KB），
  被拦的**只有这个 ajax 明细**。
- **连带影响**：`THS.count_board_zt()` 恒返回 `None` → 板块榜「涨停/全部」的**涨停家数全空**
  （实测 2026-09-18 的 `data.json`：`board_daily[].zt_count` 10/10 全是 `None`；
  `stock_total` 不受影响，它来自同花顺行业榜的「公司家数」）。
- **处置**：`zt_count` 改走**东财 push2 板块成分股**（缺口 ②，见 §5 / §3.8）。
- **同花顺其余接口一切正常**，**不要因为这一条就整体弃用同花顺**：
  `d.10jqka.com.cn`（K线 / 分时 / 实时快照）、`data.10jqka.com.cn`（涨停池 / 行业榜 / 概念榜 / 涨停原因）、
  `zx.10jqka.com.cn/event/api/getharden/`（当日强势股 + `reason` 题材归因，实测 78 只、零鉴权）、
  `news.10jqka.com.cn`（7×24 快讯）**全部可用**。
- **第三方独立印证**：`a-stock-data` skill 的 FAQ 同样记录了「同花顺板块接口 2026 年初加反爬 401，东财 push2 为替代」。

### 8.2 · 2026-09-20 其他改动备忘

- **`auction_check.py` 去东财**：删 `em_quote()`（东财 push2 快照）与 `trends_first_bar()`（**push2his** trends2 分时）；
  改为「腾讯快照 → 同花顺 `realhead` 兜底」，分时首根走 `ths_source.fetch_minute_auction()`（**带日期校验**）。详见 §3.6。
- **`fetch_data.py` 顶层键搬运**：`premarket_updated` / `auction_updated` / `auction_is_live` / `auction_source`
  此前被 15:05 的整字典重建抹掉，已修。详见 §3.6 末条。
- **死代码清理**：`L1_BOARD_CODES` / `STYLE_BOARD_KW` / `is_l2_industry()` 已从 `fetch_data.py` 删除（§9.1 第 10 条落实）。
- **⛔ `module4` 的东财成分股档：刻意保留，不要动**：同花顺成分股页 401，换过去会掉覆盖；
  东财 clist（`em_boards.fetch_board_members_em`）实测正常且能给**全量**成分股。文档原写「统一走 ths_source」是**超前描述**，
  以本节为准。
- **必看页「大盘概览」指数行精简（2026-09-20，用户点名）**：只留 上证指数 / 深证成指 / 创业板指 / 沪深300
  （白名单 `macro.INDEX_SHOW`）；科创50 / 北证50 / 中证500 / 国证2000 **停止取数**。
  上证50 / 中证1000 **仍取数但不展示**（研判3 依赖）。同时删掉死输出 `macro.style` / `STYLE_DIMS` / `build_style()`。
  详见 §3.14「大盘概览的指数行只有 4 个」与 §9.1 第 12·13 条。
  ⚠️ **改完记得「跑一次 macro.py 会改写 data.json/data.js」** —— 非交易日跑会把 `mood`（涨停/跌停家数）写成 0、
  把看板数据从上一交易日换成「非交易日」空态；**只想验证代码时，跑完要 `git checkout -- data.json data.js` 还原**。
- **三个面板顶部导语条已删（2026-09-20，用户点名）**：梯队 / 板块 / 均线的 `.panel-head > .sub` + 配套 CSS 共 6 处已清，
  见 §9.1 第 14 条。板块那条的原文「行业口径与东方财富 App 一致（申万二级行业）」**内容本身也是错的**
  （2026-09-17 已切同花顺、东财那套已废），删掉顺带消掉一处错误信息。
- **`_shot.js` 补进仓库**：§9.3 第 ③ 步一直引用它，但仓库里**此前并没有这个文件**（文档/代码脱节）。
  2026-09-20 新建，用系统 Edge（`channel:'msedge'`）逐面板截图 + 结构断言，产物落 `.shots/`（已 gitignore）。
  依赖 `playwright-core`（**不下载 Chromium**），已装到托管 workspace，见 §9.3 的命令。
- **`module4.fetch_board_members_em()` 的 `pz` 修正**：原 `pz=500` + 「条数 < 500 即尾页」→ **永远只取第 1 页**
  （每板块只拿到 100 只成分股）。已改 `pz=100` 并翻页（上限 8 页）。与 `fetch_data.em_board_zt_count` 同源问题（§9.2 第 10 条）。
- **`premarket.py` / `auction_check.py` docstring 时间口径笔误**：原写「每交易日 9:25 集合竞价结束后运行」→ 改为 **9:26**
  （9:25 只是集合竞价定格时刻，脚本在那之后才跑）。定时任务时间见 §2 / §6。
- **厄尔尼诺整块功能已删（2026-09-20，用户点名）**：后端 4 段 + 前端 4 处（CSS 2 段 / `#elnino-card` / `renderElNino()` 调用与定义）+
  `_smoke_init.js` 的函数名清单，一次清完；另删无用的 `import copy` 与两个缓存文件。详见 §9.1 第 15 条、§3.11。
  ⚠️ `data.js` 里可能残留旧的 `elnino` 键（数据文件要等下次 15:05 刷新才更新），**前端已不读它，无影响**。
- **模块3「产业板块 · 当日 TOP10」整表已从前端删除（2026-09-20，用户点名）**：卡片 `#board-daily-box` /
  标题 `#d-uptime` / 专属 `leadCell` / 该表的列宽 CSS / 全局 `.bp-cap` 三档样式全部清掉；
  **保留** 3日表 + ★ 重合 + 「小结·主线 vs 脉冲」，**保留** `data.sector_daily` 与 `ztCell`（都要复用）。
  详见 §9.1 第 16 条、§3.8b。
- **修 3日表列宽溢出（2026-09-20）**：原 9/50/26/15/22 = **122%** → 最后一列「涨停/全部」被挤成 1 个字宽、
  `0/188` 竖着折行。改为 8/34/20/16/22 = 100%，列宽只在 `nth-child` 一处定。
- 🔴 **修「连板高度梯队」整体偏高（2026-09-21，用户发现「9-17 没有 8 板」）**：
  `ths_source.fetch_zt_pool()` 的连板数改为取 `fetch_lianban_ladder(date)`（`continuous_limit_up`），
  **不再**从 `high_days_value` 自己推（原写法取错半段 + M 含断板，双重错误，见 §9.2 第 12 条）。
  影响：09-07~09-18 里修好了 5 天（如 09-17 最高板 8 → **5**、09-18 6 → **4**）。
  `lbc` 的下游（节点判定 / 断板反包 / track_status / 复盘文本）语义同步变正确。
- 🔴 **追修同一根因的「全链路残留」（2026-09-21 下午，用户发现「候选池里远望谷 6 连板」）**：
  远望谷 002161 真值 **1 板**（09-11 一字首板 → 09-14 天地板炸板 → 09-15 跌停 → 09-16 断板回封
  → 09-17 跌 → 09-18 再涨停），而 09-18 `high_days` 写「6天3板」，旧代码取低 16 位得 **6**。
  本轮把**所有同源出口**一次清干净：
  ① `fetch_zt_pool` 的**兜底**不再退回 `high_days` 的 M（改为记 1 + `lbc_src='fallback'` + 醒目告警）；
  ② **新增 `fetch_data.recount_lbc_by_continuity()`**：在 zt_hist 构建后、节点构建前，
     用历史涨停池按「严格连续」自算 —— **交叉校验权威值 + 修正接口降级值**（详见 §3 连板口径条）；
  ③ `track_status` 的「**已断板**」回退：不再沿用上一版 `data.json` 的旧值，改为从 `zt_hist`
     取「最后一次涨停那天的 lbc」（旧值会把历史错误永久固化）；
  ④ `_parse_high_days()` → 改名 **`_m_boards_from_high_days()`**，语义明确「**不是连板数**」；
  ⑤ `THS.fetch_zb_pool()`（炸板池）的 `lbc` 字段**已删**（原填 M，静默陷阱，本无消费方）→ 改名 `m_boards`。
  实测：修复后远望谷入候选池显示 **1 板**（原 6 板）；09-18 候选池全部「断板反包」票的板数
  均等于当日真实 lbc；`check_ladder.py` 两道检查全过。
  ⚠️ **连带效果（正确、但需知道）**：`lbc` 修正后**节点结构整体重算** ——
  旧口径下由假高板造出的节点（如 `CROSS_20260911 超声电子5板`、`新赛股份10板`、`楚天龙11板`）
  在  新口径下**不再产生**，同时新增一批真实节点。最终仍按 `NODE_KEEP_DAYS=10` 截到近两周。
- 🆕 **必看页 KPI 行新增「仓位建议」卡（2026-09-21，用户点名新增）**：
  在「昨日涨停」之后加第 8 个卡片，`封板率 < 75% 且 跌停 > 10 → 「空仓」`，否则「可参与」。
  实现 = `index.html` 的 `#mkt-kpi` 渲染 `cells` 数组加一项 + CSS `.kpi.judge-empty`。
  ⚠️ 判定用**原始比例**（`mood.zt_rate`）而非界面取整值；数据缺失给「—」。口径详见 §3.14「仓位建议卡」。
  `_shot.js` 已加 **7 条断言**（真值/边界/「且」语义/数据缺失/触发态样式）+ **触发态特写截图**
  `${tag}_kpi_empty.png`。`PAGE_VER` → `v2026.09.21-1`。

---

## 9 · ⛔ 已删清单 & 高频重犯错误（自查表）

> 这一节**专门针对"同一个错误反复犯"维护**。动手前扫一眼，改完对一遍。
> 成因：仓库文档、本地 memory、自动化提示词三处记忆各自漂移 → AI 照旧描述把删掉的东西加回来。

### 9.1 刻意删除的东西 —— 绝对不要恢复

| # | 已删内容 | 删除时间 | 判据 / 现状 |
|---|---|---|---|
| 1 | 「9:10 盘前新闻刷新」自动化任务（`refresh_news.py --days 3`）| 2026-09-18 | 后台已无此任务；`refresh_news.py` 文件仍在但**无人调用** |
| 2 | 必看页「消息速览」卡片（`renderNews()` + CSS）| 2026-09-18 | `index.html` grep 无 `renderNews`/`消息速览`；`data.news` 仍在产出，**仅供复盘文本引用** |
| 3 | 必看页「盘后全景·指数与量能」卡片（`renderIndex()`）| 2026-09-18 | 已由 ①`mkt-indices`/`mkt-kpi` 取代 |
| 4 | 必看页「连板晋级·昨日涨停表现」卡片（`renderBoardPerf()`）| 2026-09-18 | 同上 |
| 5 | 必看页 ①「今日资金主线 TOP6」的 **6 个卡条**（`#mkt-mainline` / `.mkt-main` / `.ml-item`）| 2026-09-18 | 只留标题 + 橙色概览条 `#mkt-mainnote`；**`macro.mainline` 字段后端仍保留**（概览要用），前端不渲染 |
| 6 | 必看页 ④「风格轮动」旧版**双向条形**（`macro.style`，用 realhead）| 2026-09-18 | 已换 RS 折线（`macro.style_rs`）|
| 7 | 东财取数路径（`jsonpGet` / `parseEm` / `push2his` K线、`auction_check` 的 push2 快照 + **push2his** trends2 分时）| 2026-09-17 / 2026-09-20 | 全站切同花顺；**保留的东财接口只有 2 处**：跌停个股池 `getTopicDTPool`、板块成分股 `em_board_zt_count`（见 §5） |
| 8 | 「妙想 mx_」补板块成分股链路 | 2026-09-17 | 改用 `ths_source.py`；`fill_mx_cache.py` 暂留但新代码别调 |
| 9 | jieba 关键词提取（新闻）| — | 已移除，不要再引入 |
| 10 | 板块榜 `is_l2_industry` / `L1_BOARD_CODES` / `STYLE_BOARD_KW` 过滤 | 2026-09-17 停用 / **2026-09-20 代码已删** | 同花顺行业体系 ≠ 东财；零调用死代码，已从 `fetch_data.py` 物理删除 |
| 11 | `_probe*.py` 等探测脚本 | 可随时删 | 19 个，临时验证工具，删了不影响主流程 |
| 12 | 必看页「大盘概览」的 **科创50 / 北证50 / 中证500 / 国证2000** 4 个指数卡 | 2026-09-20 | 用户点名删除，**已停止取数**（`macro.INDEX_SPEC` 里已移除）。⚠️ 仅剩 **上证50 / 中证1000** 仍取数（内部喂「研判3·风格」，不展示）|
| 13 | `macro.style`（风格轮动·双向条形）+ `STYLE_DIMS` + `build_style()` | 2026-09-20 | 前端 2026-09-18 已换 RS 折线；**全仓 grep 无消费方**，纯死输出，且引用了已删的科创50/国证2000 → 一并删除 |
| 14 | **梯队 / 板块 / 均线 三个面板顶部的「流程导语条」**（`.panel-head` + `.panel-head .sub` 及配套 CSS，共 6 处）| 2026-09-20 | 用户点名「没用，全部删除，不要再加」。判据：`grep -n "panel-head" index.html` 应只剩注释。**不要再以任何形式把这段说明放回去**（含卡片标题里的复述）|
| 15 | **厄尔尼诺「主题轮动地图」整块功能**（后端 `EL_NINO_DATA`/`EL_NINO_THEME_KW`/`calc_elnino_stage`/`discover_elnino_leaders`/`merge_elnino_dynamic`/`load,save_elnino_cache`/`ELNINO_DYN` + 模块3 置顶卡片 `#elnino-card` + `renderElNino()` + 全部 `.eln-*` / `.h18-*` CSS）| 2026-09-20 | 用户点名「删掉厄尔尼诺整个部分」。判据：`grep -rni "elnino" --include=*.py --include=*.html --include=*.js .` 应只剩 §3.11 / §9.1 的说明与代码里的 ⛔ 标记。缓存 `.elnino_stage_cache.json`、`.elnino_dynamic_leaders.json` 已删且从 `.gitignore` 移除 |
| 16 | 模块3「**产业板块 · 当日 TOP10**」整张表（卡片 + `#board-daily-box` + 标题里的 `#d-uptime` + 专属 `leadCell` 渲染 + `#board-daily-box` 列宽 CSS + 全局 `.bp-cap` 三档样式）| 2026-09-20 | 用户点名「删掉前端，不要在前端显示」。判据：`grep -n "board-daily-box\|d-uptime\|leadCell\|bp-cap" index.html` 应只剩 ⛔ 注释。⚠️ **`data.sector_daily` 数据保留**（3日表的 ★ 与「小结·主线 vs 脉冲」要它算），**`ztCell` 也要保留**（3日表共用）|
| 17 | 用 `high_days` / `high_days_value` 推连板数的**所有写法**（`hdv & 0xFFFF` 取天数 / `hdv >> 16` 取次数 / `_parse_high_days()`）| 2026-09-21 | 见 §9.2 第 12 条。⚠️ `_parse_high_days()` **已改名 `_m_boards_from_high_days()`**（语义＝「N天内涨停次数 M」，**不是连板数**），**只可在明确需要 M 的地方用，绝不可当连板数**。判据：`grep -rn "high_days_value" --include=*.py .` 应只剩注释。**不要再给任何 `lbc` 字段填 M** |
| 18 | `THS.fetch_zb_pool()`（炸板池）的 `lbc` 字段 | 2026-09-21 | 它原来填的是 `high_days` 的 M（**静默口径陷阱**，与 #17 同源）。炸板票的连板高度需昨日数据，单日接口给不出 → **字段已删**，改名 `m_boards`（且本无消费方）。判据：`grep -n "'lbc'" ths_source.py` 不应在炸板池函数内出现 |
| 19 | **高标跟踪的「删除按钮」及整套删除机制**：`BB_DEL_KEY` / `bbDeleted()` / `bbAddDeleted()` / document 级 click 委托 / `<button class="bb-del" data-bb-del>` / `.bb-del` `.bb-del:hover` `.bb-cur` `.bb-dropped` CSS / `localStorage['bb_deleted_v1']` / 汇总条「本机已删 N 只」 | 2026-09-21 | 🆕 用户点名「删除按钮和功能也删掉吧」。**用户原话纠正：「不是让你永久排除，只是不在跟踪了，别的地方统计数还是要用的」** → 详见 §3.15.0。判据：`grep -n "bb_del\|bbDeleted\|bb-del\|bb-cur\|bb-dropped" index.html` 应只剩 ⛔ 注释。`_shot.js` 有恒真守卫断言（`bbDel===0` / `bbCur===0` / 两个函数 `undefined` / localStorage 无 bb 键）|| 20 | **高标跟踪后端黑名单**：`bigboards_hidden.json`（**文件已 `git rm`**）、`hide_bigboard.py`（**已 `git rm`**）、`bigboards.py` 里的 `HIDDEN_FILE` 常量与 `load_hidden()` / `save_hidden()`、`build_bigboards()` 的 `hidden` 参数 | 2026-09-21 | 与 #19 同批废除。**「跨设备全栈删除」这个需求本身已被用户否定**（跌停票只是不再跟踪，不是要抹掉）。判据：仓库根目录无这两个文件；`grep -n "hidden" bigboards.py` 应只剩 ⛔ 注释 |
| 21 | 必看页 KPI 行的「**两市成交额**」卡（`data.macro.amount_yi` 的前端消费点）| 2026-09-21 | 🆕 用户点名「连同数据删掉」。KPI 卡数 **8 → 7**。判据：`grep -n "两市成交额" index.html` 应只剩 ⛔ 注释；`_shot.js` 断言 `kpiHasAmt === false`。⚠️ `macro.amount_yi` 字段本身**保留**（供复盘文本/其他模块用），只是前端不再展示 |
| 22 | 必看页 ① 面板标题里的「**与今日资金主线**」（`大盘概览与今日资金主线` → **`概览`**）| 2026-09-21 | 🆕 用户点名简化。判据：`#mkt-card .card-title` 文本含「概览」且不含「资金主线」；`_shot.js` 已断言 |
| 23 | `.sk-tab:hover` / `.sk-close:hover` 的**悬浮高亮**与 `cursor:pointer`（题材标签片的悬停反馈）| 2026-09-21 | 🆕 用户点名「不要浮动高亮」。判据：`grep -n "sk-tab:hover\|sk-close:hover" index.html` 应只剩 ⛔ 注释。触摸屏本无 hover，保留只会让人误加交互 |
| 24 | 高标跟踪卡片里**把跌停票显示出来**（含 `.bb-row.ld` 行 / 「跌停 MM-DD」绿底徽章的实际渲染）| 2026-09-21 晚 | 🆕 用户点名「你不跟踪的就不用显示了，要么太多了」。→ **显示层过滤**：`renderBigboards()` 里 `filter(s => s.status !== 'limit_down')`；**数据层不动**。判据：`_shot.js` 断言「0 行 `.bb-row.ld`」+「渲染行数 == `stats.tracking`」+ 🔴「数据层仍为全量（groups 内票数 == `stats.total`）」。⚠️ **别把过滤写进后端 `build_bigboards()`** —— 那会变成「永久排除」（见 §3.15.0）|
| 25 | 模块2「**连板候选池**」卡片（HTML 卡片 + `#cand-box` 列宽 CSS + 手机端 CSS + `renderCandidates()` + `init()` 里的调用 + `.auc-ok`/`.auc-no` 样式）| 2026-09-23 | 🆕 用户指着该表说「这个功能删掉吧，不要了」。**只删前端展示！** 判据：`grep -n "cand-box\|renderCandidates\|c2-" index.html` 应只剩 ⛔ 注释。⛔⛔ **`data.candidates` / `build_candidates()` / `premarket.py` 的写回一律保留**（竞价核对 / 推荐 / 模块4兜底 / check_ladder 四处在读）。`_shot.js` 有守卫断言：`#cand-box` 不存在 + 函数 `undefined` + **`data.candidates` 仍 >0** |

### 9.2 最常犯的 22 个错（按被纠正次数排序）
1. **按旧文档把已删 UI 加回来** —— 尤其「消息速览」与「资金主线 TOP6 卡条」。
   → 动手前先 `grep -n "renderNews\|mkt-mainline\|ml-item" index.html`，无结果即已删。
2. **同花顺指数日K 盘后不含当日** → 量能「差一天」。
   → 当日必须用腾讯 `qt.gtimg.cn` 字段 **37** 补；校验 `data.market_volume.date == 当日`。
3. **补完当日又被末尾逻辑删掉**。
   → 「排除未收盘当日」判断**只作用于日K 自身**（`in_session = k_last == today and now.hour < 15`），腾讯补的在去重后追加。
4. **触屏用原生 `title` 做提示** → 手机上完全不显示。
   → 一律用自绘 `#tip-layer`（`data-tip-title` + `data-tip` + `document` 事件委托）。
5. **浮层定位在写 innerHTML 同帧取尺寸** → `offsetWidth/Height` 为 0 → 算出负 `left`。
   → 抽出 `place()`，**先算一次 + `requestAnimationFrame` 再算一次**。
6. **RS 卡想用 realhead 快照画 20 日走势** → 快照只有当日涨跌幅，画不出。
   → 必须 `THS.fetch_daily_kline(zs_xxx, n=RS_DAYS+12, prefix='')` 走指数日K自算。
7. **混淆同花顺两套板块编码**：指数码 `881xxx/885xxx/886xxx`（K线+快照）vs 概念板块ID 6 位（取成分股）。
   → 用 `THS.board_ref()` 名称桥接，别硬套。
8. **把参考图的深色配色直接搬进来** → 本项目整体浅色，"跟整体格格不入"会被打回。
   → 结构照做、配色换浅色版（如 RS `#f87171/#4ade80` → `#dc2626/#16a34a`）。
9. **取同花顺分时首根不看日期** → 盘前 / 非交易日拿到的是**上一交易日**的分时，
   会把昨天的集合竞价当成今天的。
   → 必须校验分时 JSON 顶层的 `date`（`ths_source.fetch_minute_auction()` 已回传，
     `auction_check.ths_first_bar()` 与 `data.date` 比对，不符直接丢弃）。
10. **东财 clist 用 `pz=500`** → 东财 **`pz` 实测封顶 100**（传 500 也只回 100），
    按「返回条数 < pz 即尾页」判停会**只取到第 1 页**（`stock_total` 少一半）。
    → 必须 `pz≤100` 再翻页。⚠️ 同样写法在 `module4.fetch_board_members_em()` 里也有一份（同源问题）。
11. **以为「同花顺挂了」就整体换源** → 见 §8.1：只有**板块成分股 ajax** 被反爬拦，
    其余同花顺接口全好。整体换源会连带丢掉涨停池 / 连板梯队等**只有同花顺有**的短线核心数据。
12. 🔴 **从涨停池的 `high_days_value` 自己推连板数** → 两个错叠在一起，**「连板高度梯队」整条折线长期偏高**：
    - ① 取错半段：原写 `hdv & 0xFFFF` 取的是**低 16 位＝天数 N**，而板数在高 16 位。
      编码是 `(板数 M << 16) | 天数 N`（`8天5板`=0x50008 / `6天3板`=0x30006 / `4天2板`=0x20004）。
    - ② 就算取对高 16 位，M 也只是「N 天内的涨停次数」**含中间断板**，**不是连板数**。
      典型：众泰汽车 09-17「8天5板」+ `change_tag=LIMIT_BACK`（断板回封），被画成**当日最高 8 板**，
      而同花顺连板梯队当天最高只有 **5 板**（澳弘电子）。
    - **正确做法**：连板数一律取 `THS.fetch_lianban_ladder(date)`（`continuous_limit_up`）；
      不在梯队里（height<2 不返回）＝当日涨停不属于连续段＝**首板/断板反包，记 1**。
    - **判据 / 回归验证**：`build_ladder` 的 `top/second` 必须逐日等于该接口返回的 `height` 前两项。
      已做成常驻工具 **`python check_ladder.py`**（退出码 0=全过 / 1=有错），
      **改任何与连板数相关的代码后都要跑**。2026-09-21 修复后实测 09-07~09-18 **10/10 一致**（修复前 7 天里错 5 天）。
    - ⚠️ **教训**：只验「N == M」的样本（如闽东电力「6天6板」）会**掩盖**这个 bug ——
      09-15/09-16 恰好正确，正是因此长期没被发现。**验证要挑 N ≠ M 的样本**。
    - 🔁 **同一根因还扩散到了「连板候选池」（2026-09-21 第二次复现，用户察觉）**：
      用户发现候选池里 **远望谷被标成 6 连板**，但它 **09-11 只是一字首板**（开=高=低=收=8.04），
      09-14 天地板炸板、09-15 跌停、09-16 断板回封、09-17 跌、09-18 才再涨停 ——
      **一笔连板都没有，真值 = 1 板**。而 `high_days` 09-18 写的是「**6天3板**」，
      旧代码取低 16 位 → **6**。
      链路：`lbc` 错 → `track_status` 的 `today_lbc` 错 → 节点 `stocks[].boards` 错
      → **`build_candidates` 第二条分支原样透传 `s['boards']`** → 候选池显示 6 板。
      → 说明**同一个错值会沿"取数→节点→候选池→前端"一路传播**，修一次要**全链路复查**，
        不能只修表面那一处（本轮就把 `fetch_zt_pool` 兜底、炸板池、`track_status` 的已断板回退一起清了）。
    - 🛡️ **新增第二道纯数学断言**（`check_ladder.py --days N` 会同时跑）：
      「N天M板」且 **N ≠ M** 时，**连板数必须 ≤ M−1**。
      推理：N 天里只有 M 次涨停（N > M）⇒ 至少一天没涨停 ⇒ M 次涨停被切成 ≥2 段 ⇒ 最长段 ≤ M−1。
      **故 M 和 N 都不可能是连板数** —— 这条不依赖任何接口，能**同时抓住「取 M」和「取 N」两种错误**。
      反证实测（09-11~09-18 共 22 只 N≠M 样本）：取 N **22/22 违反**、取 M **22/22 违反**；
      修复后 **0 违反**。
    - ⚠️ **「已断板」票的板数**原写 `(prev or {}).get('boards', s['lbc'])` ——
      沿用上一版 `data.json` 的旧值会把历史错误**永久固化**。已改为从 `zt_hist`
      取「**最后一次涨停那天的 lbc**」（仍取不到才退回旧值）。

13. 🔴 **把「日K的最后一根」当成「今天」** → 盘后（15:00 后）跑管线，**`data.json` 的 `date` 回退一天**，
    今天的涨停池被挂到昨天的日期上。
    - 根因：`fetch_trade_days()` 的两个源**盘后都不含当日** —— 主源腾讯日K `qt.gtimg.cn`、
      备源同花顺指数日K `THS.fetch_daily_kline`，收盘后返回的最后一根 K 线仍是**昨天**。
    - ⚠️ **「源成功但数据陈旧」比「源全失败」更危险**：源全失败会走**缓存兜底**（缓存里反而有今天）；
      源成功但陈旧会**绕过兜底**，于是 `days[-1]` 是昨天 → `today = days[-1]` 全盘错位。
    - → 修复：`_confirm_today()` 用**涨停池**独立确认「今天是不是已经收盘的交易日」
      （`weekday()<5 and hour>=15` 且 `fetch_zt_pool(今天)` 非空），再在 `fetch_trade_days()`
      里 `if got[-1] < ds: got.append(ds)` 补上。详见 §3.16。
    - **排查口诀**：盘后 `date` 不对 → 先看日志有没有 `[warn] 交易日源缺当日`；
      再校验 `data.market_volume.date == data.date`（应恒等，且都等于当天）。
    - ⚠️ **教训**：「把最新可获取的数据当成当前」是一个**独立于数据源的假设**，
      只要上游在收盘后短暂滞后就会全线错位。**任何用"最新数据"推断"当前时点"的写法，都要配一个独立存在性校验。**

14. 🔴 **题材归类的键取错、或优先级排错** → 高标跟踪归不了类 / 归到无关板块。
    - ① **键必须是「命中的板块名」，不能是「票自己的概念名」**：用后者时 11 只票 → **11 个组**（完全不收敛，
      因为每只票的概念名都不一样）；只有落到**同一份板块榜的名称**上才会分组。
    - ② **不能按"当日最热"给涨停原因排序**：同花顺「为什么涨停」的**顺序是可靠的**，按位置取第一个命中项即可；
      改按涨幅选会跑偏（F10 一票挂 5~6 个泛概念）——
      反例：**爱仕达**按"当日最热"会归到 **土地流转**（无关），按"涨停原因优先"归到 **人形机器人**（正确）。
    - ③ **命中解析必须分档**（精确 > 去后缀相等 > 模糊包含，同档取涨幅最高）——
      不分档会让泛词「AI」被「智谱AI」之类的具体板块抢走。
    - → 实现见 `_theme_of()` 4 步优先级 + `match_board()`（§3.15）。**改归类逻辑后必看「组数是否收敛」**
      （≥4 板 12 只 → 应归到 10 个左右的题材；组数≈票数就是坏了）。

15. 🔴 **「用户下达的隐藏/排除指令」被静默吞掉** —— 早期高标跟踪黑名单踩过（2026-09-21）。
    > ⚠️ **本条教训保留，但黑名单机制本身已废弃**（用户否定了「永久排除」需求，见 §3.15.0 / §9.1 #19#20）。
    > 下面是「通用教训」，适用于**任何**「用户明确要求屏蔽/排除某物」的场景（不限于高标跟踪）。
    - 症状（历史）：`bigboards_hidden.json` 里写了 `{"hidden":[...]}`，但 `load_hidden()` 读的是 `d.get('codes')`
      → **返回空集却不报错** → 用户以为删了，票第二天又原样冒出来。
    - **教训：`dict.get()` + `or []` + 整段 `try/except` 三件套，会把"键名写错"和"文件不存在"变成同一种静默结果。**
      → 对「用户明确下达的删除/屏蔽指令」这类持久化：**读写两侧的键名必须有单一常量来源**，
        且**读取侧要兼容历史误写**。
      → 写完**必须验证真的生效**（不是只看"命令没报错"），例如重新读取一遍并断言目标已消失。
      → `save_*()` 要**保留给人看的注释字段**（如 `_note`），否则重跑一次就把说明抹掉。

16. 🔴 **「候选池入池只看 status、不看连板数」→ 池里全是 1 板 / 断板票**
    （2026-09-22 用户第二次报「连板候选池里都是断板的票」，**我只允许自己犯一次**）。
    - 症状：`candidates` 里出现一批 `boards=1` 的首板 / 断板反包票（实测 6 只），
      把真正的连板票挤出 12 个名额；用户看到的清单**整体失真**。
    - 根因：`build_candidates` 第二分支只判 `if s['status'] not in ('连板中','断板反包'): continue`，
      **没有校验 `boards`**；同一票在多个节点出现 → `dedup_candidates` 保留"优先级最高"的那条
      （= 旧节点的 `断板反包`）→ 1 板票混入。
    - **正确口径**：入池先决条件 = **当日涨停池 `lbc >= 2`**（⟺ 前一交易日也涨停）。详见 §3.6b。
    - **教训（比这次 bug 本身更重要）**：
      ① **判据要落在"数"上，不要落在"标签"上**。`status` 是派生的展示标签（可能陈旧 / 多来源冲突），
         `lbc` 才是可核验的**数值**。凡「资格筛选」，优先用数值门槛。
      ② **同一份数据在"多个来源"里出现时，去重逻辑会放大错误**：`dedup_candidates` 按优先级取一条，
         会把"旧节点里的错误标签"选出来。→ 因此**门槛要在入池时就卡死**，不能指望下游去重。
      ③ **出池前要有一次"兜底自检"**（这里是 `boards >= 2` 再过滤一遍 + warn），
         这样即便将来谁改坏某个分支，也不会漏到前端。
      ④ 🔴 **修复必须"跑数据 + 提交部署"才算完成**：上一次（09-21）我只改了代码、**没重跑生成数据**，
         于是`data.json` 里仍是旧的错误候选池，**线上一直显示错到 09-22 用户再次发现**。
         → 改完口径必须 **① 重跑生成数据的脚本 ② 断言新数据 ③ commit + push** 三步齐。
         排查「代码明明改了线上还是错」的第一步：**比对本地 `data.json` 与线上 `data.js` 的 `updated` / 关键字段**。
    - **回归检查**：`check_ladder.py` 第三道 `check_candidates()` 逐只断言 `boards >= 2`；改相关代码必跑。

17. **`except ImportError:` 里"静默返回空串" → 全站股票缩写变空（2026-09-22，用户报「缩写怎么又丢了」）**
    - **现象**：`data.json` 里 `pinyin` 字段 408 个**全是空字符串**，页面所有「缩写」列/标签空白；
      但脚本**没有任何报错**，跑得跟成功一样。用户是从页面上发现的。
    - **根因**：`fetch_data.py` 顶部
      ```python
      try:
          from pypinyin import lazy_pinyin
      except ImportError:
          def pinyin_abbr(name): return ''      # ← 灾难：静默降级成空串
      ```
      **环境重建后 pypinyin 没装回来** → 所有缩写都算成 `''`。
      `setup.bat` 只往项目内 `.venv` 装，而**实际跑的 python 并不是那个 .venv**（本机 `.venv` 根本不存在）
      → 装了也白装，且**没人会看依赖是否就位**。
    - **时间线（靠 `git show <sha>:data.json` 对比 pinyin 空值数定位）**：09-18 15:07 正常（空 8/393）
      → **09-21 15:30 起开始丢** → 09-21 16:41 起 100% 空 → 09-22 16:05 自动化跑完仍 100% 空。
      ⭐ **排查手法值得复用**：`git show <sha>:data.json` 逐个提交统计字段空值数，能直接锁定"从哪一跑开始坏"。
    - **修（三件，缺一不可）**：
      1. **装回依赖**（三个入口其实是同一个 env —— `C:\...\envs\default` 是指向 `D:\...\envs\default` 的 junction）：
         `python -m pip install pypinyin`。⚠️ **本机清华镜像不通**，必须用**官方源**（不加 `-i`）。
      2. **改掉静默降级**：`PINYIN_OK` 标志 + 运行开头大字告警 + 结尾 `股票缩写(pinyin)：N/M 非空` 自检；
         `enrich_tags.py` 同步。`setup.bat` / `refresh.bat` 改为「官方源优先 + 启动自检并自动安装」。
      3. **补数据**：重跑 `fetch_data.py` → `enrich_tags.py`（`module4.py` 视需要，A1 会丢掉 `module4` 键）。
    - ⚠️ **派生字段"沿用"陷阱**：`candidates` / `recommend` 由 fetch_data.py 从盘前版本**原样沿用**，
      所以即使装了 pypinyin、盘后重跑，这 12+1 只的缩写**依然是空**（实测残留 13 个）。
      → 已在沿用处加 **「派生字段自愈」**：`pinyin` 是名字的纯函数，沿用即按当前名字重算。
      **教训：凡是"纯函数派生字段"，在沿用/搬运处都应重算，别搬运旧值。**
    - **通用教训（与第 15 条同族）**：`try/except ImportError` + 返回空值 + 无日志 =
      把「依赖缺失」伪装成「正常的空数据」。**依赖缺失必须大声失败或大声告警**。
    - **回归检查**：`_shot.js` 断言 `股票缩写(pinyin)非空率 > 90%`（双视口跑）；`fetch_data.py` 结尾也会打印覆盖率。

18. **`except ImportError` 已列第 17 条**。**本条 = 我「按自己的臆测缩小需求」的通用毛病**（2026-09-23，用户报「22号的三板和4板呢」）
    - **表现**：把用户的口径**自作主张地理解成"够用了"的最小集**，然后按最小集实现，还自认为符合要求。
      实例：`build_ladder()` 只取 `lbcs[0]`(最高) / `lbcs[1]`(次高) 两个高度，
      前端只画三条线 → 当天存在的 **3板/4板 完全不出图**，用户一眼就看出来了。
    - **我的错误认知**：「次高板」这个名字让我以为「第二个高度就够了」——
      **纯属臆测，从未被用户确认过**。用户要的是**完整梯队台阶**（才能看出**断层**）。
    - **改正（通用）**：
      · **凡涉及"枚举 / 层级 / 全部类别"的需求，默认枚举完整集合**，不要自作主张只取 top-N 或"代表性"几条。
        要缩减，必须**先问用户**，或**在交付时显式说明我缩了什么、为什么**。
      · 有"看起来更省事的最小集"这个念头时 → 那是**危险信号**，先 `grep` 看数据里到底有几个类别、
        用户历史有没有提过其他类别。
      · **命名要抵抗歧义**：`second`（次高板）这种名字会诱导后人只取两个值 →
        新字段直接叫 `levels`（全部层级），语义上就不容漏。
    - **本条同族错误**（都是"我以为够用了"）：
      · 第 14 条同族：删了导语条却把这段说明写回卡片标题里（"换个形式放回去"）。
      · `_shot.js` 曾断言 `fmtList(list, 5)`（截断到 5 只）→ 用户报「9-15 的 2 连跌有 6 只却只显示 5 只」。
      · **规律：我对"信息量的取舍"的默认值，总是比用户想要的更小。** → 默认给全，让用户来砍。
    - **回归检查**：`_shot.js` 断言「梯队图 series 数 == 窗口层级数」「>3 条线」
      「series 名 == 各层级」；`_smoke_init.js` 断言末条含 4板/3板。

19. **「删某功能」时没分清「展示层」和「数据/流水线层」**（2026-09-23，用户要求删连板候选池）
    - **表现**：用户指着前端一张表说「这个功能删掉吧，不要了」——
      若按字面理解成"删掉 candidates"，会**连带砍掉 4 个仍在用的后台消费方**：
      `auction_check.py`（9:26 竞价核对逐只读它）/ `build_reco()`（次日推荐用它第 1 名）/
      `module4.py::get_fallback_members()`（题材兜底池）/ `check_ladder.py` 第三道（入池门槛断言）。
    - **改正（通用）**：接到"删功能"需求，**先 `grep` 全仓**，把命中点分成两类：
      · **展示层**（HTML 卡片 / CSS / `render*()` / 事件绑定）→ 用户要删的是这个，可以删。
      · **数据 / 流水线层**（后端 `build_*()` / `data.*` 字段 / 其它模块的读取）→
        **默认保留**；确有需要删的，先问用户。
      并在删除处留注释：写明「只删前端 + 列出 N 个后台消费方」，防止后人"顺手删干净"。
    - **判据**：`grep -n "cand-box\|renderCandidates\|c2-" index.html` 应只剩 ⛔ 注释；
      `_shot.js` 有守卫断言「`#cand-box` 不存在」**且**「`data.candidates` 仍 >0」。

20. **写断言时把「依赖当日数据」的文案写死 → 假失败**（2026-09-23，第 18 条同一轮又踩）
    - **表现**：`_shot.js` 里写 `if (chk.notes !== 3) failed++`（研判应为 3 条）。
      而研判 2「量价背离」**按当日数据动态产出**（需有板块 `涨幅>0.5% 且 净流出>1亿`）。
      今天没有这种板块 → 只有 2 条 → **断言红了，但代码完全正确**。
    - **这是 §9.3 第 ③ 条 "别写『某文字必然出现』的断言" 的又一次翻车。**
    - **改正**：断言的期望值必须**从数据现算**，或改为**校验结构/不变式**而非具体数值：
      · ✗ `研判应为 3 条`
      · ✓ `条数在 1~3 之间` + 每条 `tag/init/level/title/body/data` 齐全 + `level ∈ {超配,标配,观察}`
        + 正文带客观数据 + 无预测性措辞。
    - **附带**：当"期望值需要的数据前端拿不到"时（如研判 3 依赖未导出的 `上证50/中证1000`），
      **不要硬算** → 改为**内容自洽性断言**（结构 + 取值域 + 不变式），这才是稳的。

21. **把 Markdown 语法写进 HTML 文案**（2026-09-23，同轮）
    - **表现**：`index.html` 的 `.note` 里写 `当天没有该层的日子**折线直接跨过去连上**`，
      **HTML 不解析 `**`** → 页面上原样显示两颗星号，肉眼可见的脏。
    - **改正**：HTML 文案里加粗一律用 `<b>`，**绝不用 `**`**。
    - **守卫**：`_shot.js` 已加断言「梯队 note 文案无 Markdown 星号残留」。
      另可用脚本全页扫描（剥掉 `<script>` 与注释后查 `\*\*`），当前 **0 处**。

22. **🔴🔴 用户给了参考图，我却拿现有结构去硬凑 —— 同一需求连错六版**（2026-09-23，本项目最贵的一课）
    - **表现**：连板梯队图。我：
      ① v1 只画 3 条线 → 用户「22号的三板和4板呢？」
      ② v2 改成**每层一条平行线** → 用户「不能改为用折线图连接吗？」
      ③ v3 仍是 5 条平行线，只把 `connectNulls` 改 true → 用户发**参考图**「我的意思是你做成这样的折线连接」
      ④ v4 改成**单条折线**，但把同日各层**横向错开** → 用户「**同一天的纵轴不应该是垂直的吗？你这全是斜的让我怎么看哪一天是哪一天？**」
      ⑤ v5 改成单条折线 + 同日共享同一 x（竖直）→ 用户再发图「**做成这种的连接方式**」，
         我按图猜「每层一条线 + 同日横向错开」→ 用户「**你还是没懂……我就要这个样子的**」
      ⑥ **v6 才问对**：用户要的根本不是「换一种连线方式」，而是**换一套口径** ——
         「不需要最高板和次高板了；**2板以上每一层是一条线**，这样我可以看出一个**时间段内连板高度的变化**，
          **排除掉创业板**」→ 再收窄「**只统计4板以上**」+「缺层**连到 0 轴**」。✅
    - **根因（两层）**：
      · 第一层：我始终在**既有结构内打补丁**（先改线数、再改连线方式、再改 x 排布），
        每次只挪一小步，从不质疑「这张图到底要表达什么」。
      · 🔴 **第二层（更致命）**：用户说的「数据抓错了」我**当成了渲染 bug 去修**。
        我查到后端 `levels` 数据完好（09-22 有 6/5/4/3/2 全层级）就以为「不是数据问题」，
        **却没意识到「图上只画 top/second/cyb 三条线」本身就是口径错，数据再多也显示不出来**。
        用户说「其他逻辑/UI/代码都不变，不是让你重新做」= **只改口径，别改形态** —— 我又去重做了形态。
    - **口诀**：
      · **用户发参考图 → 先按图反推几何结构（几条线？x 轴是什么？同一天的点在哪？），再动代码。**
      · **当用户第二次说同一件事，说明方向错了，必须停下来重新理解，而不是继续微调。**
      · 🔴 **当用户说「数据错了」时，先分清「数据本身错」还是「口径/筛选错」还是「渲染错」——
        三者修法完全不同。尤其：数据齐全但图上看不到 → 是「口径遗漏」，不是「渲染 bug」。**
      · 「折线连接」这类词有歧义时，**主动问一句「同日是竖直堆叠还是横向铺开」**，比连改三版便宜得多。
      · **用户说「不要重新做」时 = 形态已定，只改数据口径。** 别顺手重构 UI。
    - **旁证**：同一需求用户问了 **6 次**才落地（v1→v6）。这是本项目最高的一次沟通成本。
      ⛔ **代价**：v2~v5 四版全部被回滚重写，白烧了一整天。
      **口诀升级：改这张图之前，先读 §3.1b 的「v1~v6 沿革表」，确认不要走回任一被否掉的版本。**

### 9.3 改前端后的强制三步验证（缺一步都可能线上翻车）

```bash
cd D:/软件/WorkBuddytwo/神秘力量/stock_dashboard

# ⚠️ 本机 PATH 是坏的（`node` / `python` / `ls` / `tail` 都可能 not found）→ 一律用绝对路径。
PY="D:/软件/WorkBuddytwo/.workbuddy/binaries/python/envs/default/Scripts/python.exe"   # 依赖装在这里
NODE="C:/Users/Administrator/.workbuddy/binaries/node/versions/22.12.0/node.exe"
export NODE_PATH="C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules"

# ① 抽内联 JS → Node 语法检查
"$PY" -c "import re;s=open('index.html',encoding='utf-8').read();open('_chk.js','w',encoding='utf-8').write(re.findall(r'<script>(.*?)</script>',s,re.S)[-1])"
"$NODE" --check _chk.js && rm -f _chk.js

# ② 桩件冒烟：模拟 DOM 跑 init() 与各 render*()
"$NODE" _smoke_init.js

# ③ 视觉验收：playwright-core + **系统 Edge**（不用下 Chromium）逐面板截图 + 结构断言
#    首次需装依赖（只装 core，不拉浏览器）：
#      cd "C:/Users/Administrator/.workbuddy/binaries/node/workspace" && npm install playwright-core
"$NODE" _shot.js        # 产物落 .shots/（已 gitignore），断言不过会 exit 1
```

- ⚠️ **断言里取前端数据要用裸名 `D`，不能写 `window.D`** —— 顶层 `const` 不挂到全局对象上（见 §5）。
- ⚠️ **`page.click()` 前必须先切到目标面板**：`shoot()` 最后会把页面停在「均线」，
  其他面板是 `display:none` → 点里面的元素会 **`element is not visible` 超时 30s**（真踩过）。
  加一句 `await page.click('.menu-item[data-panel="boards"]')` 再点。
- 🔴 **不能用 `document.body.innerText` 判断某段文字在不在页面里**：`innerText` **只返回可见文本**，
  `display:none` 面板里的内容**永远取不到** → 断言会**假失败**（真踩过：★ 明明在 DOM 里，`innerText` 里没有）。
  → 改用**总是存在于 DOM** 的具体容器 `.textContent`（如 `#board-3d-box`），或用 `textContent` 全文。
- ⚠️ **`page.evaluate` 里才能用 `document` / `D`**；写在 Node 作用域会 `ReferenceError`。
  需要页面事实时用 `const pg = await page.evaluate(() => ({...}))` 取回来，不要在断言里直接写 `document.x`。
- ⚠️ **不要写「某文字必然出现」的断言**（如 `★ 必须存在`）：它依赖**当天数据恰好重合**。
  两榜不重合时页面合法地显示「暂无」→ **会假失败**。→ 断言要**先按数据算出期望值**，再与页面比对
  （`data.js` 换成任意一天都成立）。
- ⚠️ `_shot.js` 的 `macro.*` 断言是拿**当前 data.js** 验的；若 data.js 还是旧版（如刚 `git checkout` 还原过），
  这些断言会失败 —— 属预期，**不要为了让它变绿去改 data.js**（会污染线上看板）。

#### 9.3.1 测 ECharts 图表的正确姿势（2026-09-22 做「涨停标黄」时踩出来）

- 🔴 **别指望截图 canvas**：headless Edge 下 `element.screenshot()` 拍 `<div>` 里嵌的 canvas 常常**全白**，
  `canvas.toDataURL()` 也可能拿不到 —— **图看着"没画"，其实数据已经上屏了**（假失败）。
- ✅ **正确做法：从 ECharts 实例读回 option 做断言**（这才是真断言"有没有上屏"）：
  ```js
  const opt = echarts.getInstanceByDom(el).getOption();
  const data = opt.series[0].data;              // 逐根 K 线：数组 = 默认色；{value,itemStyle} = 被特殊标色
  ```
  例：「涨停标黄」断言 = 只有涨停那几根不是裸数组、且 `itemStyle.color === '#facc15'`。
- ✅ **要"看图"时**：把图表渲染到**正常可见**的容器里（`position:absolute;left:-9999px` 不行、`display:none` 更不行），
  再用 `page.screenshot()` 拍**整页**（不是元素），或直接拷 `.shots/*.png` 人工看。
- ✅ **要测渲染函数本身**：直接 `page.evaluate` 调它，**喂合成数据**（不依赖网络、不依赖当天行情）——
  例 `renderKline(el, {kind:'k', bars:[...]}, '002161', '远望谷')`，再读回 option。
  比"打开真实弹层等网络"稳定得多；真实行情只用于**最后跑一次目视核对**。
- ⚠️ 直接调渲染函数会留下全局实例（如 `skChart`）→ 测完 `skChart = null; echarts.dispose(el);` 复位，否则 resize 会炸。

- 触屏验证：`newPage({ hasTouch:true, isMobile:true })` + `.heat-cell` 点击 → 断言 `#tip-layer` 带 `on` 类。
- ⚠️ **新增/删除卡片容器 id 时，同步改 `_smoke_init.js` 里的 id 列表**（漏了会空引用）。
- ⚠️ **改完必须 `git add -A && git commit && python push_api.py`** —— 不推就白改。
- **改前端后 `PAGE_VER` 要 +1**（`index.html` 顶部 `const PAGE_VER`，显示在页脚，用来确认浏览器加载到最新版）。

