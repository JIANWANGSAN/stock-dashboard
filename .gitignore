# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 本地工具目录
.workbuddy/

# 视觉验收截图（`_shot.js` 的产物，可随时重跑，不入库）
.shots/

# 运行期缓存（可自动重建，不必入库）
.seal_cache.json
.stock_cache.json
.module4_kline_cache.json
.mx_members_cache.json

# 体积大且可重建的中间产物
review_dump.json

# 自动化跑复盘时落盘的临时中间文件与当次复盘草稿
#   —— 规则：下划线开头的 .py/.json 一律视为当次临时产物，不入库
#   —— 例外：_tags_dump.py 是盘后复盘 B 段的**正式工具**（给涨停/炸板/跌停池补概念标签），必须入库
_*.py
!_tags_dump.py
_*.json
_*.html
_review_today.md

# 系统
.DS_Store
Thumbs.db
